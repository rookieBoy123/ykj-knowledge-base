# 知识库更新日志 2026-07-03（第6轮）

## 本日新增

### 新增业务流程文档（7篇）

- **新增「中药图谱管理流程」**：完整描述医学百科中中药图谱的内容管理流程。覆盖中药名称（20字限制）+ 药味多选（字典 `zytj-yw`）+ 适应症多选（字典 `zytj-syz`）+ 封面图上传（200×200px/1:1/2MB限制）+ 功效作用（100字必填）+ 四个必填标签页（基本用法/药物作用/用药方法/注意事项，均使用富文本编辑器）+ 少填标签页自动跳转提示 + 单条删除二次确认 + 批量删除无选择静默忽略。代码来源：operate/medicalEncyclopedia/chineseMedicineIllustration/index.vue（270行）+ addOrEdit/index.vue（250行）。

- **新增「经典名方管理流程」**：完整描述医学百科中经典中医处方的内容管理流程。覆盖处方名称搜索（20字限制）+ 处方类型（字典 `jtmf-lx`）+ 适应症（字典 `jtmf-syz`）+ 书籍出处（字典 `jtmf-sj`）+ 功效作用富文本 + 封面图上传 + 新增/编辑页跳转模式（非弹窗模式）+ 批量删除。代码来源：operate/medicalEncyclopedia/classicFamousPrescriptions/index.vue（283行）+ addOrEdit/index.vue（269行）。

- **新增「疾病知识管理流程」**：完整描述医学百科中疾病知识的内容管理流程。覆盖疾病名称搜索（20字）+ 疾病分类（字典 `jbzs-jbfl`）+ 疾病详情富文本 + 弹窗模式新增/编辑（800px宽弹窗）+ 新增成功后自动回到第一页 + 编辑后保持当前页 + 批量删除。代码来源：operate/medicalEncyclopedia/diseaseKnowledge/index.vue（243行）+ AddOrEdit.vue（136行）。

- **新增「药膳食疗管理流程」**：完整描述医学百科中药膳食疗配方的管理流程。覆盖药膳名称（20字）+ 食材筛选的特殊处理（编码→中文名映射转换查询）+ 功效多选（字典 `yssl-gx`）+ 适应症多选（字典 `yssl-syz`）+ 功能作用（300字必填）+ 主料（100字必填）+ 辅料（50字可选）+ 做法（300字必填）+ 食物拓展（300字可选）+ 封面图 + 弹窗模式编辑（650px宽）。代码来源：operate/medicalEncyclopedia/medicatedDietTherapy/index.vue（322行）+ AddOrEdit.vue（192行）。

- **新增「经络穴位管理流程」**：完整描述医学百科中经络穴位的内容管理流程。覆盖穴位名称搜索（10字限制）+ 归属经络（字典 `jlxw-jl`）+ 身体部位（字典 `jlxw-bw`）+ 封面图 + 功效作用富文本 + 弹窗模式编辑 + 批量删除。代码来源：operate/medicalEncyclopedia/meridiansAcupoints/index.vue（283行）+ AddOrEdit.vue（179行）。

- **新增「不良反应事件管理流程」**：完整描述医疗不良事件的记录与追踪流程。覆盖事件业务类型（字典 `sjywlx`）+ 就诊人姓名搜索（5字限制）+ 上报时间范围筛选 + 新增弹窗（850px宽）含14个必填字段：事件业务类型/事件发生时间（禁选未来日期）/上报医生（全量加载搜索）/就诊人（选择后自动带出手机号，通过会员接口获取真实号码）/上报时间/事件标题（50字）/事件类型（字典 `eventType`）/事件等级（字典 `eventLevel`）/事件影响级别（字典 `eventImpactLevel`）/事件描述（3000字）/主要原因（3000字）/采取措施（1000字）/不良影响（1000字）/解决措施（3000字）/发生地址（100字）+ 手机号格式校验（1开头11位正则）+ 详情查看弹窗 + 编辑回显。代码来源：operate/adverseEvent/index.vue（202行）+ components/addOrEdit.vue（456行）+ components/detail.vue（68行）。

- **新增「用户协议管理流程」**：完整描述平台用户协议的内容管理流程。覆盖协议不可新增（仅可编辑已有协议）+ 编辑弹窗（800px宽）含协议标识（编辑时置灰/255字限制）+ 协议类型（字典 `xylx`/编辑时置灰）+ 标题（255字）+ 协议正文富文本 + 标题空校验 + 标识空校验 + 标识重复校验（代码已写但当前版本注释未启用）+ 批量删除（代码中已注释不可用）。代码来源：operate/article/agreement/index.vue（291行）+ components/add.vue（389行）。

### 新增常见问题（6条）

- 问题 302-307：覆盖医学百科功能入口权限、中药图谱详情区四个标签页全部必填的校验逻辑、药膳食疗食材筛选编码转换机制、不良反应事件手机号自动填充的依赖条件（患者需绑定会员）、用户协议不支持新增的设计原因、中药图谱编辑必填字段汇总。

### 覆盖度变化

- 业务流程：93 → 100 篇
- 常见问题：301 → 307 条
