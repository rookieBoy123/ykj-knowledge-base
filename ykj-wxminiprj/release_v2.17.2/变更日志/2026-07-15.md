# 变更日志 2026-07-15

## 变更概览
本日合并 v2.17.2 分支大规模更新，涉及 193 个文件变更，新增 11600 行代码，删除 4132 行。

## 新增功能

### 1. 支付宝支付支持
- 新增健康顾问端支付宝支付引导页面（`packageConsultant/pages/aliPay/index.vue`），含扫码支付步骤指引
- 患者端 HIS 缴费页面完善支付宝支付方式支持
- 新增支付宝相关图标资源（扫码、下载、引导步骤图等）

### 2. 线下电子病历查看（医生端）
- 新增 `packageDoctor1/pages/elMedical/offline.vue`（524行），支持查看从HIS同步的线下病历
- 展示8字段病情信息 + 7项体格检查，支持折叠展开
- 支持原始病历预览

### 3. 线下处方详情查看（医生端）
- 新增 `packageDoctor1/pages/rpDetail/offline.vue`（511行），支持查看HIS同步的线下处方
- 支持成药和中药双处方类型模板切换
- 支持电子处方预览
- 可从患者端健康档案或医生端聊天室病历卡片两个入口进入

### 4. HM视频转诊
- 新增 `packagePatient2/pages/hmVideoTransfer/index.vue`（85行），支持视频转诊功能入口

### 5. 自定义树形选择器组件
- 新增 `uni_modules/custom-tree-select` 组件（880行），支持多级树形数据选择

### 6. 药品订单新增"创建时间"字段
- 药品订单列表和详情页新增显示"创建时间"

### 7. 快速开方成功页跳转
- 快速开方完成后跳转至成功页面，改善用户操作反馈

### 8. 预约接诊超时提示
- 预约接诊模式下，当天预约支持超时提示文案

### 9. 公共代码模块重构
- 新增 `packageCommon` 公共分包，将人脸识别、手机号登录等通用页面迁移至此
- 提取 `getServiceName` 公共方法，消除10处重复代码
- 提取 `lastMessageFormat` 公共方法，消除重复代码
- 新增 `inquiry.js` 和 `tim.js` 公共工具文件
- 新增 `caHelper.js`（87行）CA签章公共辅助方法

## 优化改进

### IM即时通讯
- 优化IM登录稳定性，修复会话列表准确性
- 清理孤儿房间逻辑优化
- 修复医生首页拉取会话时 `isSyncCompleted` 可能为 false 的问题
- 患者角色跳过获取处方数量接口调用，减少不必要的请求

### 医生首页
- 重构医生首页吸顶效果
- 修复医生首页会话同步状态判断问题

### 视频通话（TRTC）
- 重构 TRTC 房间组件，删除旧的 user-controller/pusher/stream/user 模型
- 重构病历盒子组件（806行），删除旧的 memberInfo/patientInfo/visitInfo 子组件
- 报告盒子组件大幅改进（272行）
- 优化视频房间页面逻辑

### 问诊聊天
- 问诊聊天组件大幅重构（944行变更），改进消息列表和输入区域
- 自定义消息组件改进（200行变更）
- 常用语组件改进（83行变更）
- 群聊倒计时组件优化

### 处方模块
- 处方添加/编辑页面重构（148行变更）
- 处方详情组件优化（99行变更）
- 处方模板新增搜索功能

### 会诊模块
- 会诊科室选择页面重构（166行变更）
- 会诊申请页面优化（119行变更）
- 患者信息详情组件优化

### 电子病历
- 电子病历详情就诊日期改为就诊时间，统一问诊时间字段显示
- 电子病历编辑页优化（56行变更）

### 患者端
- 医生首页重构（156行变更），接诊标签页大幅改进（359行变更）
- 患者角色基本信息组件改进（217行变更）
- 报告查看页面重构（检查报告341行、检验报告333行）
- HIS缴费页面大幅改进（328行变更）
- 代付详情页新增登录重定向路径保存逻辑
- 药品建议详情页改进（148行变更）
- 发票申请页改进
- 物价公示页改进（206行变更）
- 患者端首页重构（134行变更）

### 医网信CA签章
- 升级医网信插件版本 v1.5.4 → v1.5.7
- 更新环境标识配置

### 其他
- 我的问诊字段样式宽度调整
- 已完成和已预约列表从详情页返回时不再重复请求第一页
- 医生身份认证页重构（155行变更）
- 评价页改进（43行变更）
- 就诊记录卡片优化
- 医生详情页改进
- 预约确认页改进
- 健康档案线下记录组件改进（317行变更）

## 代码来源
- 合并提交 `d63f188ed`（release/v2.17.2 → release/v2.16）
- 共 193 个文件变更，涵盖小程序全部主要分包
