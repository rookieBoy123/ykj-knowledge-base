# 变更日志 — 2026-07-01

## 初始创建（第一轮深度分析）

服务端知识库从零开始建立。本轮从以下 Java 服务模块深度分析得出业务的完整后端视角。

### 新建基础文档（3个）

- **「功能模块总览」**：完整梳理30个微服务模块的职责和模块间关系。涵盖会员（member）、问诊（inquiry）、订单（order）、会诊（consultation）、药品（medicine）、医保（yb）、随访（followup）、量表（scale）、医院（hospital）、HIS对接（his）、系统管理（system）、预约（reservation）、即时通讯（im）、实时音视频（rtc）、CA签章（ca）、人脸识别（faceid）等全部模块。

- **「常见问题库」**：从服务端代码反推10个业务FAQ，覆盖订单0元自动完成机制、支付前防重复历史渠道补单、退款申请唯一性限制、医保退款分步处理、医生拒诊取消链路、药师审核在处方流程中的位置、会诊与问诊的数据模型差异、订单过期自动取消时效、手机号加密存储与绑定规则、药品缺货/禁用的区分逻辑。

- **「变更日志」**：本文件。

### 新增业务流程（5个）

- **新增「订单生成与支付处理流程」**：完整描述从订单创建到支付完成的完整后端链路。覆盖订单号生成规则（O+年月日+7位随机数）、订单来源类型判断（问诊/处方/预约/会诊/服务包等6种）、0元订单的特殊处理（跳支付直接完成的不同状态分支）、支付渠道组装（微信JSAPI/扫码/支付宝/医保四种渠道的优先级和可用性检查）、支付前防重复查单（历史渠道补单机制）、医保支付的可用性校验（医生资质+患者定点）、支付回调处理（验签→更新状态→记录流水→触发后续业务）、药品订单的发货签收流程（15天自动签收+取货码签收）、订金订单的分段支付。代码来源：ykj-service-order/OrderCommandServiceImpl.java（1251行）+ OrderQueryServiceImpl.java（352行）+ WxPaymentServiceImpl.java（274行）。

- **新增「退款审核与处理流程」**：完整描述退款申请→审核→执行的完整流程。覆盖退款申请创建的前置校验（订单状态/支付流水/同一流水唯一申请）、审核通过/拒绝的分支处理（原路退回调用支付接口 vs 手动退款标记完成）、医保退款的特殊步骤（先退医保再退自费的分步逻辑）、退款失败重试机制（允许重试的状态白名单REFUND_STATUS_ALLOW_RETRY）、退款申请单状态流转（待审核→审核通过/拒绝→退款完成/失败）。代码来源：ykj-service-order/RefundApplyServiceImpl.java（423行）+ OrderCommandServiceImpl.insuranceRefund()方法。

- **新增「问诊生命周期管理流程」**：完整描述问诊从发起到结束的服务端全流程。覆盖问诊接诊（acceptInquiry的IM同步处理+事件发布）、问诊结束（finishInquiry的收益计算+评价触发）、问诊取消（cancelInquiry的订单关联取消+退款触发+是否推送事件控制）、转诊流程（已接诊转诊+未接诊退回协助两种分支）、预约问诊的排班关联。代码来源：ykj-service-inquiry/InquiryCommandServiceImpl.java（144行）。

- **新增「处方开具与审核签章流程」**：完整描述处方全生命周期的后端逻辑。覆盖处方草稿保存/更新（门诊号去重+加载已有草稿）、处方提交校验（药品/诊断完整性）、药师审核（DoctorApproval和PharmacistApproval双审批状态）、电子签章（医网信/源康健PaaS/放心签三平台）、处方PDF生成（含签章图片叠加）、处方模板存储（中药/成药/公用三类存储差异）。代码来源：ykj-service-inquiry/PrescriptionCommandServiceImpl.java（750行）+ ykj-service-medicine/DrugsServiceImpl.java（848行）。

- **新增「会诊生命周期管理流程」**：完整描述会诊从发起到结束的完整流程。覆盖会诊创建（分配单号+记录多医生+关联订单）、超时处理（30分钟未审核自动取消+15分钟未支付自动取消）、会诊前提醒（提前10分钟TRTC+IM群组通知）、视频房间管理（TRTC房间创建+入场/出场）、会诊报告填写（主治医生报告 vs 参会医生意见的填写顺序+二次确认）、CA签章（多平台支持）、状态流转（待审核→待开始→进行中→待报告→完成/取消）。代码来源：ykj-service-consultation/ConsultationServiceImpl.java（791行）。

以上新增内容从以下核心服务模块深度分析得出：ykj-service-order（订单模块，OrderCommandServiceImpl 1251行 + RefundApplyServiceImpl 423行 + OrderQueryServiceImpl 352行 + WxPaymentServiceImpl 274行）、ykj-service-inquiry（问诊/处方模块，InquiryCommandServiceImpl 144行 + PrescriptionCommandServiceImpl 750行）、ykj-service-consultation（会诊模块，ConsultationServiceImpl 791行）、ykj-service-member（会员模块，MemberServiceImpl 238行）。

## 补充更新（第二轮深度分析）

### 新增业务流程（5个）

- **新增「处方结算与费用计算流程」**：完整描述处方下单后的费用计算与订单生成后端链路。覆盖预结算（费用明细计算含药品费/加工服务费/运费/订金四项分项）、正式结算（防重复检查→重新预结算→创建订单→回填关联）、已创建订单查询（支付渠道/医保信息展示）、医保结算专用流程（费用明细上传→取消历史重复→授权码处理→个账支付判断→诊费处理）、报销明细查询。代码来源：ykj-service-inquiry/PrescriptionSettlementServiceImpl.java（901行）。

- **新增「电子病历创建与签章管理流程」**：完整描述电子病历的全生命周期后端逻辑。覆盖创建病历（医生/患者信息自动组装→诊断保存→签章记录创建→分享二维码生成）、修改病历（签名状态重置→诊断差分更新→删除保护→重新签章）、追加诊断（去重处理→不可修改标记）、更换患者、绑定会员（唯一绑定校验）、主动签署（重复签章复用）、作废撤回（多断言器授权检查→原因记录→逻辑删除）、查询（多维度筛选→诊断自动组装）。代码来源：ykj-service-inquiry/MedicalRecordServiceImpl.java（851行）。

- **新增「服务预约订单生成与核销流程」**：完整描述服务预约从生成到核销的完整后端链路。覆盖预约单生成（可预约性校验→费用计算→预约单号生成→0元/付费状态分支→订单创建→发票创建）、支付回调（状态切换→核销码生成→通知推送）、核销流程（核销码查单→状态校验→执行核销→订单完成→自动开票）、取消与退款（状态白名单→取消状态映射）、详情查询（权限控制→信息聚合→患者双重数据源）。代码来源：ykj-service-reservation/ServiceReservationsServiceImpl.java（826行）。

- **新增「医生排班管理与号源控制流程」**：完整描述医生排班的后端管理逻辑。覆盖保存排班（多级校验含时间冲突/当前冲突→班次编号分配→号段保存→旧数据清理）、号源发布（批量操作→未发布筛选）、停诊处理（全天/指定班次两种范围→影响号段级联→原因记录100字限制→停诊事件推送）、号源使用与释放（扣减前二次校验→30分钟截止时间限制→并发保护）、排班自动同步（定时任务→15天后→模板应用→跳过已有排班）、查询（管理后台全量/小程序端仅已发布已启用未停诊）。代码来源：ykj-service-hospital/DoctorScheduleServiceImpl.java（788行）。

- **新增「外部机构对接问诊流程」**：完整描述第三方机构通过开放平台对接问诊服务的后端链路。覆盖发起问诊（机构身份校验→问诊方式分支→患者信息必填→病史选填→病历图片转存→扩展信息传递）、完整上传创建（会员注册→患者保存→问诊单创建含IM群组→病历生成→订单创建→聊天记录回填→自动结束）、取消问诊（机构权限校验→状态白名单→退款控制→通知控制）、更新问诊信息（病情描述/病历图片同步更新）、视频记录查询。代码来源：ykj-service-open/OpenInquiryServiceImpl.java（907行）。

### 新增常见问题（8个）

- **FAQ #11**：处方结算时为什么提示"已存在订单信息"？（防重复下单机制+已生成订单的查找和继续支付处理）
- **FAQ #12**：处方结算的总金额是怎么算出来的？为什么有时比药品加起来便宜？（四项费用分项说明+订金/包邮导致金额变化的解释）
- **FAQ #13**：医生保存病历后，为什么有的需要签章有的不需要？（CA认证配置+强签已移除+修改重置签名三种场景）
- **FAQ #14**：病历修改时删除诊断，为什么提示"不允许删除已经被关联的诊断"？（处方引用保护+医保上传一致性+替代方案）
- **FAQ #15**：服务预约的核销码是什么格式？在哪里可以看到？（8位随机码+三种生成时机+核销码为空的四种原因）
- **FAQ #16**：医生排班的"号源"是如何扣减的？为什么有时预约不了？（扣减时机+并发保护+取消释放+四种预约失败原因）
- **FAQ #17**：停诊后，已经预约了该医生的患者会收到通知吗？（停诊事件推送+两种停诊范围+通知内容）
- **FAQ #18**：外部机构对接问诊时，系统怎么分配医生？（精确匹配→模糊匹配→不存在提示+对接模式的特点说明）

以上新增内容从以下核心服务深度分析得出：ykj-service-inquiry/PrescriptionSettlementServiceImpl.java（处方结算，901行）+ MedicalRecordServiceImpl.java（电子病历，851行）、ykj-service-reservation/ServiceReservationsServiceImpl.java（服务预约，826行）、ykj-service-hospital/DoctorScheduleServiceImpl.java（医生排班，788行）、ykj-service-open/OpenInquiryServiceImpl.java（外部对接，907行）。

## 补充更新（第三轮深度分析）

### 新增业务流程（2个）

- **新增「量表测评答案处理与评分流程」**：完整描述患者逐题作答量表时的后端校验与自动评分逻辑。覆盖逐题保存的四重校验（题目存在性/单选题多选限制/选项合法性/已完成保护）、最后一题自动触发评分机制（judgeLastOne → generateResult）、得分组装的三层数据结构（题目选项映射+题目维度映射+答案列表）、总分与维度分累计算法（选项序号→分值映射+多选题支持+维度分组聚合）、测评完成后的答案不可修改保护（先检查result再决定是否拦截）。代码来源：ykj-service-scale/ScaleAnswerServiceImpl.java（167行）+ ScaleEvaluateRecordServiceImpl.java（96行）。

- **新增「随访任务发送与状态管理流程」**：完整描述随访定时任务的执行链路和内容状态动态校验机制。覆盖sendFollowupTask定时任务的完整执行路径（任务存在性校验→内容状态更新→发送决策→状态变更→订阅消息推送）、患教/问卷的发送时动态禁用检查（setForbidden方法批量比对Status.Published）、内容全部禁用的取消逻辑（Canceled状态+不发送通知）、有可用内容时的推送流程（组装PatientReplyMessage→构建NotificationSendDTO→调用统一通知服务）、医生和会员信息的查询失败保护（记录日志+静默跳过）。代码来源：ykj-service-followup/FollowupTaskServiceImpl.java（197行）。

### 新增常见问题（6个）

- **FAQ #19**：量表答题时为什么提示"已测评完成，不能再次修改答案"？（result检查+防篡改+继续vs重新两种方案说明）
- **FAQ #20**：量表测评的总分是怎么计算出来的？（选项分值映射+多选题累加+维度分JSON存储+计算示例）
- **FAQ #21**：随访任务发送时，如果某个患教或问卷被管理员禁用了，会发生什么？（动态检查机制+三种结果+设计意图说明）
- **FAQ #22**：随访任务状态中"待随访"和"已随访"各代表什么含义？（四状态对照表+患者感知差异+后台可见性）
- **FAQ #23**：量表测评"继续答题"和"重新测评"有什么区别？（五维对比表+recordId变化+旧答案处理+旧结果保留）
- **FAQ #24**：随访任务的定时提醒是怎么触发的？医生需要手动操作吗？（自动触发机制+sendFollowupTask定时任务+医生无需操作+查看路径）

以上新增内容从以下核心服务深度分析得出：ykj-service-scale/ScaleAnswerServiceImpl.java（量表答题评分，167行）+ ScaleEvaluateRecordServiceImpl.java（测评记录，96行）、ykj-service-followup/FollowupTaskServiceImpl.java（随访任务，197行）。

## 补充更新（第四轮深度分析）

### 新增业务流程（2个）

- **新增「问诊预约与排班匹配流程」**：完整描述患者预约挂号时系统如何进行多维度的重复预约校验。覆盖三种校验场景（商城问诊/快速问诊/预约挂号的不同匹配规则）、禁止医生问诊自己的身份校验（AI问诊例外）、同一天同医生排班日期匹配逻辑、问诊单四种生命周期状态流转、超时自动结束定时任务机制（含转诊/取消/结束三种分支）。代码来源：ykj-service-inquiry/InquiryAppointServiceImpl.java（918行）。

- **新增「患者实名认证与就诊人管理流程」**：完整描述就诊人从新增到认证、从绑定到管理的后端全链路。覆盖四种证件类型的格式校验规则（大陆身份证/护照/港澳/台湾）、并发锁防重复创建、FaceID实名认证条件分支（开关控制）、身份证自动提取性别和出生日期、拼音首字母生成、会员一对一绑定（本人关系限制）、地址自动同步收货地址、就诊人编辑的信息合并逻辑（同身份记录自动合并）、解绑的短信验证码流程、医保慢病查询的前置条件（必须实名+有险种）。代码来源：ykj-service-hospital/PatientServiceImpl.java（815行）。

### 新增常见问题（5个）

- **FAQ #25**：为什么同一个患者不能对同一位医生在同一天预约两次？（三种重复校验场景+日期匹配规则+预约挂号限制+替代方案）
- **FAQ #26**：为什么有的就诊人能使用医保，有的不能？（实名认证+医保险种+慢病资格三层条件+联系医保局建议）
- **FAQ #27**：问诊单为什么会自动结束？结束的规则是什么？（定时任务开关+三种分支处理+转诊/取消/结束差异+医生未接诊场景）
- **FAQ #28**：为什么新增就诊人时提示"同一就诊人只能绑定在一个手机号"？（一对一绑定规则+100600错误码+解绑流程+防止数据混乱的设计意图）
- **FAQ #29**：医保退款为什么有时成功有时失败？退款的正确处理流程是什么？（先退医保再退自费的分步逻辑+同一流水唯一申请+重试白名单+授权中断场景）


## 补充更新（第五轮深度分析）

### 新增业务流程（2个）

- **新增「会诊签章与PDF报告生成流程」**：完整描述会诊结束后五种签名类型的电子签章和PDF生成后端逻辑。覆盖五种签名类型的身份校验规则（APPLICATION=主治医生/INVITATION=受邀医生/OPINION=受邀医生/REPORT=主治医生/REVIEW=不校验）、重复签名的自动撤销机制、CA签章与手写签名的双路径（enableCA开关控制）、手写签名的PDF嵌入（Beetl模板+20%缩放+不同签名类型的位置差异化）、PDF报告包含的完整信息（会诊信息/患者信息/主治+受邀医生签名/诊断/治疗方案）、审核超时30分钟自动取消和签章超时12小时的提醒机制。代码来源：ykj-service-consultation/ConsultationSignServiceImpl.java（627行）。

- **新增「药品库管理与同步流程」**：完整描述平台药品库的中药/成药两大类药品的后端管理逻辑。覆盖手动录入的去重校验（中药按drugsAlias检重/成药按多字段组合检重）、批量Excel导入（覆盖更新策略+失败记录错误原因）、第三方药房同步（阿康AKANG接口+每页100条+7000毫秒连接超时）、药品搜索（拼音相似度排序/管制药品黑名单拦截提示"该药品为精麻毒等管制类药品，按规定不可线上开方"）、药品完整性校验（库存单位/换算比例/药品库信息存在性检查）、药品状态（启用/禁用/黑名单三级管控）。代码来源：ykj-service-medicine/MedicalDrugsServiceImpl.java（922行）。

### 新增常见问题（4个）

- **FAQ #30**：会诊签章时提示"非主治医生无法签字"是什么意思？（五种签名类型身份校验对照表+两种典型错误提示+对应关系说明）
- **FAQ #31**：会诊签章中的CA签章和手写签名有什么区别？什么时候用哪种？（5维对比表+优先级规则+适用场景+配置开关控制）
- **FAQ #32**：为什么药品搜索时，某些药品搜不到，提示"该药品为精麻毒等管制类药品"？（黑名单机制+搜索前拦截+合规限制+线下替代方案）
- **FAQ #33**：第三方药房的药品数据同步失败或超时怎么办？（5步排查流程+超时阈值+批量导入容错+偶尔vs持续性差异处理）

以上新增内容从以下核心服务深度分析得出：ykj-service-consultation/ConsultationSignServiceImpl.java（会诊签章，627行）、ykj-service-medicine/MedicalDrugsServiceImpl.java（药品库管理，922行）。

## 补充更新（第六轮深度分析）

### 新增业务流程（2个）

- **新增「会员注册与信息管理流程」**：完整描述平台会员注册、信息更新的服务端全链路。覆盖注册流程的去重检查→分布式锁防并发（FairLock+3秒等待+429反馈）→事务中完成ID生成（SeqService序列）、默认头像拼接（OSS域名+配置路径）、默认昵称生成规则（"会员"+memberId+手机后4位）、医生邀请标记（isSpecial区分）；更新流程的昵称/头像非空更新；换手机号的格式校验+唯一性校验（加密查询）+微信OpenId重置；OpenId绑定（先解绑再关联）和解绑（批量清空）机制；积分增减的@Transactional保护。代码来源：ykj-service-member/MemberServiceImpl.java（238行）+ ApiMemberController.java（116行）。

- **新增「发票创建与查询流程」**：完整描述平台发票管理的服务端逻辑。覆盖订单发票创建（POST /api/order/invoices，insert分组校验+memberId自动获取）、订单发票查询（GET按订单号返回完整OrderInvoices对象）、服务预约发票（EInvoice系列接口含图片URL/红冲/作废）、管理后台发票导出（MgmtOrderInvoicesController的多条件查询+9列Excel导出）、打印按钮的条件显示（pictureNetUrl非空）。代码来源：ykj-service-order/ApiOrderInvoicesController.java（39行）+ InvoiceExportVo。

### 新增常见问题（5个）

- **FAQ #34**：为什么有的就诊人能绑定医保，有的不能？（三层条件：实名认证+医保险种+慢病资格+数据同步延迟说明）
- **FAQ #35**：会员注册时为什么提示"请求频率过高，请稍后再试"？（分布式锁机制+并发保护+锁超时+设计意图）
- **FAQ #36**：会员更新手机号时，为什么提示"手机号码已被使用"？（唯一性校验+格式校验+微信解绑+管理员后台同理）
- **FAQ #37**：订单发票是如何创建和查看的？（创建接口+查询接口+预约发票+管理后台导出）
- **FAQ #38**：会员注册时的昵称和头像是怎么生成的？（默认昵称规则+默认头像路径+医生邀请标记+后续可修改）

以上新增内容从以下核心服务深度分析得出：ykj-service-member/MemberServiceImpl.java（会员管理，238行）、ykj-service-order/ApiOrderInvoicesController.java（发票管理，39行）、ykj-service-member/ApiMemberController.java（会员接口，116行）。

## 补充更新（第七轮深度分析）

### 新增业务流程（3个）

- **新增「外部机构药品订单对接流程」**：完整描述第三方医疗机构通过开放平台对接药品下单的完整后端链路。覆盖预结算（费用明细计算含药品金额/运费/总金额+第三方药房与平台库存同步）、锁定库存（第三方药房下单锁定+5分钟有效期+超时自动取消+平台库存扣减）、创建订单（处方双重审核校验/处方有效期校验/配送方式校验/预结算vs直接创建两种路径）、支付流水处理（微信支付/线下支付双渠道+支付单号生成规则）、发货与物流（机构主动发货取件码核销/快递公司编码校验+定时拉取第三方物流+自动确认发货）、取消订单（第三方订单取消+库存还原+自动退款处理）、过期预结算单定时清理（取消第三方订单+释放库存+标记已取消）。覆盖从预结算到退款的完整生命周期，含15+种异常场景处理。代码来源：ykj-service-open/OpenOrderServiceImpl.java（831行）。

- **新增「微信医保支付对接流程」**：完整描述平台通过微信医保支付接口处理医保支付的完整后端链路。覆盖三种消费类型分支（纯个账/统筹+现金/统筹+个账）、统一下单（参数组装含金额元转分+身份证MD5加密+支付类型判断+沙箱环境切换）、预支付记录保存（等待回调初始状态）、授权码获取（二维码+微信OpenId+HmacSHA256签名）、支付结果双通道获取（主动查询WXYBPayedEvent发布+被动回调WXYBPaymentCallbackController）、退款接口（applyForRefund/refundQuery）、订单关闭释放额度、对账单下载、身份证实名核验（80005→100500错误码转换）。代码来源：ykj-service-yb/ykj-service-yb-wx-biz/.../WXYBServiceImpl.java（482行）。

- **新增「问诊自动归档流程」**：完整描述问诊结束后系统自动归档所有医疗资料的后端逻辑。覆盖双事件触发器（问诊结束InquiryFinishedEvent+审方完成PrescriptionAuditEvent）、可归档校验（问诊已完成+无未审处方）、创建归档记录（目录路径生成规则→患者姓名+证件后4位/问诊单号+日期）、定时任务自动归档（每次100条+四种资料逐一处理：病历HTML转JPG图片含诊断/处方PDF转储中药西药命名不同/图文聊天JSON序列化/视频MP4转储带序号）、每项失败独立处理+整体状态Fail/Success、管理员手动重试（仅Fail状态可操作）、归档列表搜索（问诊单号/患者姓名右模糊+状态筛选）。代码来源：ykj-service-archiving/ArchiveLogServiceImpl.java（343行）。

### 新增常见问题（5个）

- **FAQ #39**：外部机构对接下单时，为什么提示"机构未设置需要药品下单，不能生成订单"？（neededDrugOrder开关+pharmacyId配置+两项检查步骤+管理员配置建议）
- **FAQ #40**：外部机构下单时提示"处方未审核通过不允许创建订单"，怎么处理？（双重审批要求+两种审核状态含义+处方有效期限制+排查方法）
- **FAQ #41**：医保支付时提示"100500错误"是什么意思？（微信实名核验失败+80005→100500转换+三种原因+解决方案）
- **FAQ #42**：问诊归档为什么有的问诊结束了但没有归档记录？（两个触发条件+状态与审方双重限制+定时任务异步处理+时间延迟说明）
- **FAQ #43**：归档失败了怎么办？可以重新归档吗？（手动重试操作+四种失败原因+状态校验限制+文件存储格式说明）

以上新增内容从以下核心服务深度分析得出：ykj-service-open/OpenOrderServiceImpl.java（外部订单，831行）、ykj-service-yb/ykj-service-yb-wx-biz/.../WXYBServiceImpl.java（微信医保，482行）、ykj-service-archiving/ArchiveLogServiceImpl.java（归档，343行）。

## 补充更新（第八轮深度分析）

### 新增业务流程（1个）

- **新增「开放平台处方对接流程」**：完整描述第三方医疗机构通过开放平台提交处方并关联订单的后端链路。覆盖处方接收的三层校验（机构身份确认→医生精确匹配→药品对齐校验）、处方状态流转（草稿→医生审批→药师审批→审核通过/不通过）、处方的双重审核机制（DoctorApproval+PharmacistApproval两级审批确保安全合规）、订单关联的前置条件（处方有效期+审核通过+未重复使用）、订单状态回调通知（支付/发货/取消状态变更）。代码来源：ykj-service-open/OpenPrescriptionServiceImpl.java（646行）。

### 新增常见问题（3个）

- **FAQ #44**：外部机构对接处方时提示"医生不存在"是什么原因？怎么解决？（三种原因+四步排查+管理员核对建议）
- **FAQ #45**：处方的双重审核（医生+药师）各审什么？不通过怎么办？（两级审核职责说明+拒绝原因返回+修改后重新提交）
- **FAQ #46**：为什么有的处方创建订单时提示"处方未审核通过"？（三个前置条件逐一说明+处方使用限制+有效期概念）

以上新增内容从以下核心服务深度分析得出：ykj-service-open/OpenPrescriptionServiceImpl.java（开放处方，646行）、ykj-service-inquiry/InquiryAppointServiceImpl.java（问诊预约，918行）。

---

## 第二轮补充（20:00）

### 新增内容

- **新增「小程序页面装修管理流程」**：完整描述后台页面装修功能的后端服务链路。覆盖页面分组管理（底部分组BottomGroup的增查改+权限diy:group:read/write）、页面完整CRUD（新增/编辑/查询/分页/删除+权限diy:page:read/write）、使用页面操作（同一类型唯一使用标记+DiyPageType枚举）、预览二维码生成与重置（调用微信小程序码API）、患者端查询接口（按类型/按ID双模式）。代码来源：ykj-service-system/MgmtDiyPageController.java（86行）+ MgmtDiyGroupController.java（56行）+ ApiDiyPageController.java（35行）。

- **新增「导诊同步流程」**：完整描述外部系统（如HIS）通过管理后台接口同步导诊记录的后端链路。覆盖患者信息与监护人信息的合并组装（BeanUtils.copyProperties两次合并）、导诊详情（过敏史/现病史/既往史/病情描述/病历图片URL列表）、监护人关系的GuardianRelation枚举、无监护人时的分支处理（null跳过合并）。代码来源：ykj-service-guidance/MgmtGuidanceController.java（102行）。

- **新增「订阅消息模板管理流程」**：完整描述小程序订阅消息模板的查询与同步机制。覆盖模板查询接口（GET /api/subscribe/templates 按appId获取）、WxMiniProperties多小程序appId区分、订阅模板的使用场景（问诊状态通知/会诊进度/订单状态/物流发货/随访提醒）。代码来源：ykj-service-subscribe/ApiSubscribeController.java（31行）。

### 新增常见问题

- FAQ #47-#52：页面装修"发布使用"vs"预览"区别、同类型页面唯一使用原因与首页替换、导诊同步数据来源与用途、订阅消息模板配置位置、微页面分发方式与看不到的原因、慢病病种名称不可修改的数据来源

---

## 补充更新（第九轮深度分析）

### 新增业务流程（3个）

- **新增「AI医生问诊代理服务流程」**：完整描述平台后端如何作为中间人桥接患者与AI代理服务的全链路。覆盖aiDoctorEnabled总开关的统一拦截机制（全AI功能统一走一个开关、关闭时静默降级返回默认值）、开始问诊的患者信息组装与pcId生成、对话的SSE流式传输实现逐字回复效果、五种AI能力的独立调用（对话/处方OCR/语音转写WebSocket/报告生成/状态更新）、管理后台多条件分页查询（日期/年龄/姓名/业务类型）、对话历史查询、问诊结束状态更新、所有异常统一兜底返回空值不抛错的设计策略。代码来源：ykj-service-inquiry/AiAgentServiceImpl.java（433行）。

- **新增「处方电子签章管理流程」**：完整描述处方签章的全生命周期后端逻辑。覆盖签章编号生成规则（TR_SIGN+日期+7位随机数递归防重复）、CA签章vs手写签名的双路径分支（enableCA开关决定）、医生vs药师两种签章角色的差异化处理（签章关键词"医生："vs"审方："、业务类型创建vs追加、签章位置坐标Y轴差异）、CA签章创建（患者信息组装→签章人信息获取→CA服务调用→caUniqueId保存）、前往签章的状态校验（PENDING唯一+已签署拦截）、完成签章的CA查询结果嵌入（签章图片获取+签章数据保存+状态更新）、审批不通过的REJECT标记、签章撤销的级联操作（本地状态变更+CA服务远程删除+try-catch单条保护）、源文件查找的优先级策略（OSS→本地→空字符串）。代码来源：ykj-service-inquiry/PrescriptionSignServiceImpl.java（380行）。

- **新增「药房注册与绑定管理流程」**：完整描述药房管理的后端核心逻辑。覆盖新增药房的三重校验（手机号非空+名称去重+手机号唯一绑定）、第三方药房对接（创建/编辑+已启用不可逆保护）、排序分配机制（maxOrder+1）、中药加工服务联动保存、线下药房自动账号创建（查找→注册→授权→绑定四步自动流程）、编辑时的变更追踪（名称去重排除自身+手机号变更自动解绑旧用户绑定新用户）、用户药房绑定的唯一性控制（一对一同手机号+不支持多药房切换）、解绑的角色依赖检查（有其他线下角色则不删除）、启用/禁用的即时生效、四种排序操作（排最前/排最后/上移/下移+边界保护）、系统配置强控的查询模式（默认药房强制限定+线下药房统一屏蔽）、药品类型查询的去重聚合、药品数量的独立统计更新。代码来源：ykj-service-medicine/PharmacyServiceImpl.java（512行）。

### 新增常见问题（8个）

- **FAQ #53**：AI医生的回复为什么有时候很慢，甚至会"卡住"？（SSE流式传输/代理服务负载/aiDoctorEnabled开关被关闭，三种原因）
- **FAQ #54**：为什么有的医院看不到AI医生？怎么开通？（aiDoctorEnabled总开关+AI账号创建+静默降级特性）
- **FAQ #55**：AI医生的OCR识别为什么失败？如何提高识别准确率？（图片质量/文件损坏/无文字可识别+操作建议）
- **FAQ #56**：处方签章中的CA签章和手写签名有什么区别？什么时候用哪种？（6维对比表+法律效力+实现方式+使用条件+审方处理）
- **FAQ #57**：处方签章时提示"处方已被签署"是什么意思？（PENDING状态唯一+医生vs药师独立+撤销后需重新创建）
- **FAQ #58**：为什么新增药房时提示"该手机号已绑定药房"？（一对一绑定+新增/编辑双重校验+解决方案）
- **FAQ #59**：药房禁用后，已经开出的处方还能在该药房配药吗？（即时生效+已开处方不受影响+历史订单正常+可随时重新启用）
- **FAQ #60**：AI问诊失败时为什么没有任何错误提示？（静默降级设计+兜底默认值+日志记录+开关一键关闭）

以上新增内容从以下核心服务深度分析得出：ykj-service-inquiry/AiAgentServiceImpl.java（AI代理服务，433行）、ykj-service-inquiry/PrescriptionSignServiceImpl.java（处方签章，380行）、ykj-service-medicine/PharmacyServiceImpl.java（药房管理，512行）。

## 补充更新（第十轮深度分析）

### 新增业务流程（1个）

- **新增「问诊预约锁定与签到管理流程」**：完整描述患者预约挂号后从锁号→使用→签到→提醒的全生命周期后端逻辑。覆盖五种预约取消场景（锁号超时自动取消/签到截止取消/患者主动取消/问诊取关联动取消/停诊批量取消）、两个定时任务（handleReservationLockTimeout=锁号超时处理+checkInCutoffCancelReservation=签到截止处理）、预约前10分钟双端提醒（患者端预约号+时间/医生端患者姓名+时间）、签到的IM单聊通知+订阅通知双通道、停诊事件的批量取消与try-catch单条保护。代码来源：ykj-service-inquiry/InquiryReservationServiceImpl.java（533行）。

### 新增常见问题（3个）

- **FAQ #61**：预约挂号后多久必须完成支付？超时了会怎样？（锁号机制+定时任务自动取消+号源释放+二次校验+不同类型锁号时长差异）
- **FAQ #62**：预约签到有时间限制吗？如果错过了签到时间会怎样？（10分钟前可签到+截止时间计算+自动取消+退款+取消问诊联动+主动取消建议）
- **FAQ #63**：AI辅助决策功能为什么有时提示"30分钟后重试"？（3次失败触发锁定+30分钟锁定+成功重置+功能总开关+仅影响当前问诊单）

以上新增内容从以下核心服务深度分析得出：ykj-service-inquiry/InquiryReservationServiceImpl.java（问诊预约+签到，533行）、ykj-service-inquiry/SuggestionServiceImpl.java（AI辅助决策，453行）。

## 补充更新（第十一轮深度分析）

### 新增业务流程（1个）

- **新增「AI辅助决策与病历建议流程」**：完整描述AI为医生提供提问建议和病历辅助的服务端完整链路。覆盖两个独立功能（提问建议getQuestions+病历辅助getMedicalRecord的各独立开关控制questionAiSuggestion/medicalAiSuggestion）、4步处理流程（开关检查→锁定检查→失败计数检查→构建请求调用AI）、三层数据源优先合并策略（患者基础病史→AI预问诊病史仅已生成报告→已有病历病史，defaultIfBlank不覆盖已有）、聊天记录组装（IM历史消息→过滤医生/患者→仅文本消息→角色映射）、病情描述优先级（病历描述>问诊单描述）、失败锁定机制（30分钟内3次失败→锁定30分钟→成功立即清零，两个功能独立计数互不影响）、病历辅助诊断对齐（AI返回名称→标准诊断库匹配→过滤未匹配项）、所有子数据源获取失败的静默降级（catch异常记录日志跳过不影响主流程）。代码来源：ykj-service-inquiry/SuggestionServiceImpl.java（453行）。

### 新增常见问题（3个）

- **FAQ #64**：AI提问建议和AI病历辅助为什么有时可以正常使用，有时却没有任何响应？（双开关控制+3次失败锁定机制+两个功能独立计数）
- **FAQ #65**：AI病历辅助生成的诊断为什么可能和医生手动输入的不完全一样？（标准诊断库匹配对齐+AI名称→标准编码过滤+未匹配项被丢弃的原因）
- **FAQ #66**：AI辅助决策的聊天记录是从哪里获取的？会泄露患者隐私吗？（IM群组文本消息+只取医生/患者+数据传输加密+不传输姓名身份证号）

以上新增内容从以下核心服务深度分析得出：ykj-service-inquiry/SuggestionServiceImpl.java（AI辅助决策，453行）。
