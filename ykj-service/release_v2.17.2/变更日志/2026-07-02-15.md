# 变更日志 2026-07-02（第十五轮）

## 第二十八轮深度分析

### 新增业务流程（3个）

- **新增「电子病历创建与签署流程」**：完整描述病历从创建到签署完成的后端处理逻辑。覆盖创建流程（组装医生/患者数据→判断中西医保类型→初始签名状态未签→保存诊断含名称非空校验+标准名称覆盖+疾病诊断校验→创建签署记录含业务编号BL+日期+随机数→CA开关判断）、CA签名处理（生成待签PDF含医院印章→调用CA服务创建签署含患者信息+医生CA账号+签名定位坐标X/Y/Width/Height）、更新流程（签名状态重置→诊断变更增量处理：找出删除诊断+新增诊断→删除前关联校验"不允许删除已经被关联的诊断"）、完成签署（状态校验须为签署中→CA签名取回签署后PDF解码/非CA附加医生手写签名→上传OSS→更新病历状态+签署记录状态均为已签）、诊断关联保护（被关联诊断标记不可修改unalterable→多业务源汇总绑定诊断ID→删除和编辑权限控制）、病历与会员绑定（已绑定不可重复绑定/会员不存在异常）、病历撤销（断言链校验+原因记录+软删除）。异常处理：10种详细校验提示。关联模块：ykj-service-inquiry/ykj-service-ca/ykj-service-member/ykj-service-hospital。代码来源：ykj-service-inquiry/MedicalRecordServiceImpl.java（851行）。

- **新增「医生排班管理流程」**：完整描述支撑在线问诊预约的排班核心服务。覆盖核心概念（班次+号段双层级/三状态isRelease+isStopReception+isEnabled）、保存流程六阶段（医生+班次参数校验→班次名称+时间双重冲突校验→号段时间范围+号段重叠校验→当天排班特殊校验：过期号段不可新增+过期号源数不可修改→生成UUID编号先存号段后存班次→删除旧数据）、排班发布（isRelease=false→true批量更新/无待发布提示）、停诊处理（全天vs指定班次/已有挂号必填停诊原因≤100字→设置isStopReception=true→发布停诊事件通知）、号源扣减退还（使用：校验号段+班次三状态+预约时限号段结束前30分钟/号源二次校验→alreadyUsed±1/取消：不校验停诊→alreadyUsed-1+alreadyCancel+1）、定时同步（仅同步医生列表+排班模板→目标日期当前+15天→检查已有排班跳过→异步逐医生处理）、患者端查询过滤（仅返回已发布+未停诊+启用）。异常处理：15种校验提示含"预约不能超过号段结束时间前30分钟""班次信息发生变更请刷新""小于当前时间的排班不允许操作"。代码来源：ykj-service-hospital/DoctorScheduleServiceImpl.java（788行）。

- **新增「处方预结算与下单流程」**：完整描述处方药品结算的后端处理逻辑。覆盖预结算流程五步骤（查处方状态校验→组装药房信息→组装收货地址快递/自提→确认服务加工费如代煎→计算费用项：药品费+加工费+运费+定金→最终金额负数归零）、正式结算（校验已有订单→重新预结算服务费不合并→创建订单含药品明细+收货地址+费用项+来源类型→支付渠道分流：普通微信/支付宝vs医保统筹/个人账户/微信医保→生成处方PDF含患者+诊断+药品+签名+印章）、医保结算特殊处理（授权码查询→费用拆分医保+自费→明细上传→微信医保小程序支付）。异常处理：已存在订单拦截、处方状态校验。关联模块：ykj-service-inquiry/ykj-service-order/ykj-service-medicine/ykj-service-yb/ykj-service-member。代码来源：ykj-service-inquiry/PrescriptionSettlementServiceImpl.java（901行）。

### 新增常见问题（7个）

- **FAQ #230**：医生创建病历后为什么提示需要进行"电子签名"？（合规要求+CAvs手写+签名状态重置）
- **FAQ #231**：为什么修改病历时提示"不允许删除已经被关联的诊断"？（处方关联保护+不可修改标记+可新增）
- **FAQ #232**：病历的"签署中"状态是什么意思？（中间状态+CA认证等待+不重复创建签署记录）
- **FAQ #233**：医生排班中的"号段"和"班次"有什么区别？（双层级结构+号段归属+号源独立+时间范围+不重叠）
- **FAQ #234**：为什么医生排班保存后患者端看不到？（isRelease默认false+发布操作+三条件过滤）
- **FAQ #235**：医生停诊时什么情况下必须填写停诊原因？（已挂号人数>0触发+100字限制+患者通知）
- **FAQ #236**：预约时提示"预约不能超过号段结束时间前30分钟"是什么意思？（时限限制+举例+服务端校验）

以上新增内容从以下核心服务深度分析得出：ykj-service-inquiry/MedicalRecordServiceImpl.java（电子病历，851行）、ykj-service-hospital/DoctorScheduleServiceImpl.java（医生排班，788行）、ykj-service-inquiry/PrescriptionSettlementServiceImpl.java（处方结算，901行）。
