# 变更日志 2026-07-03 (第13轮)

## 新增业务流程
1. **系统配置批量更新与历史还原流程（服务端）** — 分析 `MgmtSysSettingController.java`（69行）及 `SysSettingMgmtServiceImpl.java`（151行）。涵盖配置项存在性校验、值合法性校验（SettingValidator）、相同值跳过优化、批量更新事务、历史记录自动生成、版本还原递归调用、SettingUpdatedEvent事件发布。
2. **访问密钥生成与安全存储流程（服务端）** — 分析 `MgmtSysAccessKeyController.java`（100行）及 `SysAccessKeyServiceImpl.java`（150行）。涵盖用户存在性校验、密钥数量上限（2对）、UUID生成AccessKey ID/Secret、SHA-256加盐哈希加密存储、EasyExcel生成CSV文件上传OSS、最近使用时间更新、明文仅返回一次的安全设计。
3. **Webhook回调注册与事件驱动推送流程（服务端）** — 分析 `MgmtSysWebhookController.java`（74行）及 `SysWebhookServiceImpl.java`（126行）。涵盖系统启动缓存初始化（@PostConstruct）、event→webhooks映射缓存、Redis 1小时过期策略、CRUD后主动清除缓存、事件触发时缓存命中/未命中双路径、批量删除与缓存联动。

## 新增常见问题
- 322：系统配置批量更新时，如果其中一个配置项不存在会怎样？
- 323：系统配置的"相同值跳过"机制有什么作用？
- 324：访问密钥的AccessKey ID有什么特殊含义吗？为什么是32位？
- 325：为什么密钥文件要上传到对象存储（OSS）而不是直接返回文件内容？
- 326：Webhook的Redis缓存为什么设置为1小时过期？
- 327：删除Webhook后，已经排队的回调请求还会发送吗？
- 328：Webhook支持哪些事件类型？事件名称的格式是怎样的？
- 329：为什么同一个事件类型可以注册多个Webhook地址？

## 代码来源
- `.../controller/mgmt/MgmtSysSettingController.java` (69行)
- `.../service/impl/SysSettingMgmtServiceImpl.java` (151行)
- `.../controller/mgmt/MgmtSysAccessKeyController.java` (100行)
- `.../service/impl/SysAccessKeyServiceImpl.java` (150行)
- `.../controller/mgmt/MgmtSysWebhookController.java` (74行)
- `.../service/impl/SysWebhookServiceImpl.java` (126行)
