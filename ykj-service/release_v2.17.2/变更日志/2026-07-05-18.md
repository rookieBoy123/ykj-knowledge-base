# 知识库变更日志 2026-07-05（第18轮）

## 新增业务流程（1篇）

| 文档 | 来源代码 |
|------|---------|
| 订单退款完成与跨渠道退款分派流程（服务端深挖） | `OrderCommandServiceImpl.completeRefund()` + `RefundApplyServiceImpl` 全部 |

## 新增常见问题（8条）

| 编号 | 问题 | 来源文件/代码 |
|------|------|-------------|
| 603 | 为什么取消订单时会为每一笔收入流水分别创建退款申请？ | OrderCommandServiceImpl.cancelOrder() L249-274 |
| 604 | 为什么订单完成时会同时设置收货时间和完成时间？ | OrderCommandServiceImpl.manualReceive() L453 + completeOrder() L682 |
| 605 | 为什么退款完成时用了LambdaUpdateWrapper而不是乐观锁？ | OrderCommandServiceImpl.completeRefund() L631-643 |
| 606 | 退款申请单号是如何生成和防重的？ | RefundApplyServiceImpl.generateOutRefundNo() L411-422 |
| 607 | 为什么退款回调事件需要同时携带支付渠道参数？ | RefundApplyServiceImpl.handleRefundedEvent() L276-278 |
| 608 | 为什么预支付时会先清除旧的预支付单？ | OrderCommandServiceImpl.cleanupBeforePrepay() L619-623 |
| 609 | 为什么申请退款金额超过实际支付金额时系统不报错？ | OrderCommandServiceImpl.refundApply() L311-315 |
| 610 | 为什么verifyInquiryOrder现在会主动查询第三方支付状态？ | ApiOrderController.setDeferredResult() L217-225 |

## 代码来源（近期提交）

- `2b3d1f9c27` fix: 完善对支付宝支付的支持（16文件、312行新增）
  - `OrderCommandServiceImpl.java`：PAYED_STATUS映射、multi-flow退款、completeRefund重构、originFlowStatus、expirationTime检查、cleanupBeforePrepay
  - `ApiOrderController.java`：expireOrder端点、cancelPayment端点、第三方支付状态主动查询
  - `OrderConstant.java`：PAYED_STATUS map、REFUND_RESULT_COMPENSATION_MINUTES
  - `settings.yaml`：支付宝配置从func迁移到payment
- `33fdf948ca` feat: 医生api详情接口新增返参（CurrentDoctorVO新增consultationType + isDelayCheckIn）

## 备注

本轮深度聚焦 `OrderCommandServiceImpl`（1308行）和 `RefundApplyServiceImpl`（423行）两个核心服务类，重点挖掘：取消订单的多流水退款处理、退款申请单的生成与防重、跨支付渠道退款分派、支付回调丢失的补偿机制、退款金额软容错策略等细节。
