# 变更日志 2026-07-03 (第12轮)

## 新增业务流程
1. **设备信息管理流程（服务端）** — 分析 `MgmtInsDeviceController.java`（63行）及 `InsDeviceServiceImpl.java`（76行）。涵盖设备编号自动生成（SBBH+时间戳+随机数）、新增时关联机构信息、按设备名称/型号模糊搜索、按产地/使用情况精确筛选、权限模型（hosp:devices）。
2. **素材管理流程（服务端）** — 分析 `MgmtSysMaterialController.java`（84行）及 `SysMaterialServiceImpl.java`（119行）。涵盖素材批量保存（URL域名去除/文件名提取）、分类+子分类层级查询、素材移动（跨分类迁移）、批量删除、OSS域名拼接还原。
3. **用药建议处方状态流转与审核流程（服务端）** — 结合小程序端 `drugsuggest/detail.vue`（1816行）及服务端 `ApiPrescriptionController.java`（901行）、`PrescriptionCommandServiceImpl.java`（750行）。涵盖createFlowStatus和prescriptionStatus双状态机组合、支付条件控制（afterApprovalPayment开关）、处方有效期判断、中药药品明细隐藏规则（medicineUnitHide）、CA签名平台选择、会员绑定校验。

## 新增常见问题
- 314：设备编号 SBBH 后面的数字是什么含义？
- 315：素材管理中移动素材到新分类后原来的分类还能看到被移动的素材吗？
- 316：用药建议的处方为什么会有两个审核状态？
- 317：什么情况下处方不能复用（引用处方）？
- 318：素材批量保存时如果中间有一条失败会怎样？
- 319：处方过期后还能查看药品明细吗？
- 320：为什么有的处方不需要先审核就能支付？
- 321：素材管理中"分类"和"子分类"的关系是怎样的？

## 代码来源
- `.../controller/mgmt/MgmtInsDeviceController.java` (63行)
- `.../service/impl/InsDeviceServiceImpl.java` (76行)
- `.../controller/mgmt/MgmtSysMaterialController.java` (84行)
- `.../service/impl/SysMaterialServiceImpl.java` (119行)
- `.../controller/api/ApiPrescriptionController.java` (901行)
- `.../service/impl/PrescriptionCommandServiceImpl.java` (750行)
