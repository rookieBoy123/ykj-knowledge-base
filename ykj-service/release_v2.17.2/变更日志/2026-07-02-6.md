# 变更日志 2026-07-02（第六轮）

## 第二十四轮深度分析

### 新增业务流程（3个）

- **新增「CA电子签章用户认证与管理流程」**：完整描述平台与第三方CA电子认证服务机构对接的全流程。覆盖CA账号创建（姓名+身份证加密存储→生成25位Base64用户编号→同步第三方平台获取OpenId→状态设为Authing）、CA账号认证（发起认证→第三方回调异步处理→更新认证状态/签章图片/失败原因→发布UserAuthEvent事件通知）、密码管理（MD5加密存储+首次自动生成默认签章→医生姓名转Base64图片→配置修改权限受CA平台策略控制）、签章设置（修改前验证密码→密码错误提示"您的密码验证不通过"→可自定义上传签章）、手机号变更（加密新号→同步第三方→重置认证状态→同步失败提示"变更手机号码失败，请稍后重试"）、账号注销（状态设为NotAuth）。多平台策略模式（CaUserStrategy/CaSignStrategy→支持医网信/云凯健等）。代码来源：ykj-service-ca/CaUserServiceImpl.java（411行）、CaSignServiceImpl.java（162行）。

- **新增「小程序订阅消息模板同步与发送流程」**：完整描述平台通过微信小程序订阅消息向患者推送服务通知的全链路。覆盖模板同步（Redis分布式锁防并发→类目资质校验→读取YAML配置schema→三方数据对比DB/API/YAML→自动新增/更新同步→刷新Redis缓存→不可删除原则"DB有API无可记录不可删"）、消息发送（@Async异步→根据消息类型取模板→通过@Keyword(index)注解匹配字段顺序→调用wxService发送→记录发送日志成功/失败→失败不阻塞主业务）、日志清理（定时任务每3个月删除旧日志）。异常处理：类目未添加提示"请登录小程序管理后台添加"、模板被删记录日志不删DB、关键字不匹配记录错误日志。模板缓存：Redis Hash结构key=subtmpls:{appId}。代码来源：ykj-service-subscribe/SubscribeTemplateServiceImpl.java（321行）、SubscribeMessageServiceImpl.java（145行）。

- **新增「导诊记录创建与使用流程」**：完整描述患者通过第三方渠道导诊后平台记录导诊信息供医生使用的流程。覆盖导诊创建（第三方调用guidanceSyn→创建患者档案→处理病历图片OSS转存内部ID→计算年龄→生成UUID编号→保存监护人信息→返回唯一编号）、医生使用（useArchivesByUniqueId→查询导诊记录→校验资源存在→状态更新为已使用→记录医生ID→返回患者档案）、患者补全（upRecordPatient→更新patientId→返回补全后信息）。状态流转：未使用(Unused)→医生接诊→已使用(Used)。异常处理：参数异常提示"参数异常"、资源不存在提示"资源不存在"、同步失败提示"同步导诊记录失败"、监护人新增失败。代码来源：ykj-service-guidance/GuidanceServiceImpl.java（155行）。

### 新增常见问题（8个）

- **FAQ #160**：CA签章和普通签名有什么区别？（法律效力+防篡改+认证流程+签章图片+多服务商）
- **FAQ #161**：为什么CA认证状态一直是"认证中"？（未完成认证+回调未到达+可能原因+解决方案）
- **FAQ #162**：为什么处方上没有显示我的签章？（认证状态+签章设置+密码+签署记录）
- **FAQ #163**：订阅消息模板同步失败怎么办？（类目未添加+模板被删+同步锁+配置问题）
- **FAQ #164**：为什么患者收不到订阅消息？（未订阅+模板不匹配+openId+频次限制+版本设置+日志排查）
- **FAQ #165**：导诊编号是什么？从哪里获取？（UUID格式+跨系统传递+创建接口返回）
- **FAQ #166**：为什么导诊记录显示"资源不存在"？（编号无效+未创建+传递错误）
- **FAQ #167**：CA签章的手机号变更后需要重新认证吗？（同步新号+重置为Authing+失败不影响旧号）

以上新增内容从以下核心服务深度分析得出：ykj-service-ca/CaUserServiceImpl.java（411行）、ykj-service-ca/CaSignServiceImpl.java（162行）、ykj-service-subscribe/SubscribeTemplateServiceImpl.java（321行）、SubscribeMessageServiceImpl.java（145行）、ykj-service-guidance/GuidanceServiceImpl.java（155行）。
