# 变更日志 2026-07-04（第4轮）

> 增量更新：深度挖掘服务端未覆盖模块

## 变更概览

| 变更类型 | 内容 | 影响范围 |
|---------|------|---------|
| 新增流程 | 科室诊断对照与父子级联同步流程 | 标准信息模块 |
| 新增流程 | 字典分类编码唯一性校验流程 | 系统模块 |
| 新增FAQ | FAQ #392-395（4条） | 常见问题库 |

## 详细变更

### 1. 科室诊断对照与父子级联同步流程（新增）
- 深入分析 StdDeptDiagnoseServiceImpl（425行）中的父子级联同步机制
- 新增诊断时的向上递归同步逻辑（子科室→父科室→根节点）
- 删除诊断时的智能清理逻辑（检查兄弟科室是否仍保留）
- 更新操作 = 删除+新增的组合处理
- 科室编码层级规则（点号分隔的树形结构）
- Excel 批量导入机制（Redis 缓存+异步线程池）
- 数据源依赖说明（标准科室字典 + ICD-10 诊断信息）

### 2. 字典分类编码唯一性校验流程（新增）
- 分类唯一性校验接口 `POST /mgmt/dictionary/_check-exists`
- 内容编码唯一性校验接口 `POST /mgmt/dictionary/content/_check-exists`
- 前端集成逻辑：新增时必校验，编辑时仅修改时校验
- 完整的接口权限汇总表（10个接口）

### 3. FAQ 新增（4条）
- FAQ #392：子科室添加诊断后父级科室是否自动更新
- FAQ #393：Excel导入科室诊断时部分行被跳过
- FAQ #394：字典分类"编码已存在"的处理方法
- FAQ #395：字典内容"编码已存在，请重新输入"的原因

## 代码来源

- `StdDeptDiagnoseServiceImpl.java`（425行）— 科室诊断对照核心业务
- `MgmtStdDeptDiagnoseController.java`（67行）— 科室诊断对照接口
- `MgmtStdMedController.java`（96行）— 标准药品接口
- `MgmtStdMedDiagnoseController.java`（66行）— 标准药品诊断对照接口
- `MgmtDictionaryController.java`（131行）— 字典管理接口
