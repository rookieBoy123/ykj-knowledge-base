# 变更日志 2026-07-02（第二轮）

## 第二十轮深度分析

### 新增业务流程（2个）

- **新增「订单创建与支付准备流程」**：完整描述订单模块订单生命周期的核心后端逻辑。覆盖订单创建的订单号生成规则（O+yyyyMMdd+7位随机数）、0元订单三分支特殊处理（处方→已支付/待发货→待发货/其他→已完成）、测试环境金额保护（非0元非医保→实付0.01）、过期时间计算（默认24小时可自定义）、关联数据写入（商品明细+收货地址+收费项）、事件发布通知；支付准备的四重校验（已支付拦截错误码40001+已失效拦截错误码40002+历史渠道查单防重复支付→逐渠道查第三方真实状态自动补单+医保支付三条件控制）、支付渠道构建（微信默认选中排第一+支付宝+医保条件显示）、支付金额计算（总额减已付）；订单取消的状态校验（不可取消状态列表+已完成仅问诊/预约可取消）、处方订单通知第三方可选、退款申请自动创建（已支付→退款中→自动退款双模式）、超时取消标记；自动签收的15天扫描机制（发货超15天+未签收+处方订单+每次100条）、取货码核销（校验码+状态+记录核销人）。代码来源：ykj-service-order/OrderCommandServiceImpl.java（1251行）。

- **新增「问诊激活与超时处理流程」**：完整描述问诊从激活到超时自动流转的完整后端逻辑。覆盖重复问诊防创建（同会员+同患者+同医生+同服务类型+待支付/问诊中状态→拒绝/商城问诊仅校验需求编号/预约挂号校验同天号段/快速问诊类型独立校验）、不能问诊自己账号下医生的保护；问诊激活流程（存在校验+启用聊天+系统消息推送+可选自动接诊）、可用性双校验（未支付→不可用/禁用聊天室→不可用/无群聊ID→不可用）；角色管理（主治医生Main+服务医生Service+患者+IM账号构建）；超时自动处理（定时任务+autoEnd开关+指定ID匹配）、服务医生场景超时分支（主治接诊中→自动转诊 / 医助接待中→自动取消→标记医生拒绝接诊+通知）、无服务医生场景直接结束；患者端四类列表查询（已预约/问诊中/已完成/待支付→各自独立条件和排序规则）、消息列表三状态展示。代码来源：ykj-service-inquiry/InquiryAppointServiceImpl.java（918行）。

### 新增常见问题（7个）

- **FAQ #137**：订单支付时为什么提示"当前订单已支付！"？（历史渠道查单机制+补单拦截+防重复支付设计）
- **FAQ #138**：为什么有些订单不需要支付就直接完成了？（0元订单三分支处理+常见场景举例）
- **FAQ #139**：订单过期后还能恢复使用吗？（不可恢复+重新下单+定时扫描说明）
- **FAQ #140**：为什么问诊支付完成后还不能立即开始聊天？（激活步骤+三种原因+排查建议）
- **FAQ #141**：问诊为什么会自动取消？提示"超过接诊时间"？（医助接待超时+转诊/取消分支+autoEnd开关）
- **FAQ #142**：为什么同一患者不能重复发起相同医生的同类型问诊？（防重复支付+防资源浪费+四维校验+三种特殊场景）
- **FAQ #143**：问诊中「已预约」「问诊中」「已完成」三个状态有什么区别？（三维对比表：含义/包含内容/排序方式）

以上新增内容从以下核心服务深度分析得出：ykj-service-order/OrderCommandServiceImpl.java（订单命令服务，1251行）、ykj-service-inquiry/InquiryAppointServiceImpl.java（问诊预约服务，918行）。
