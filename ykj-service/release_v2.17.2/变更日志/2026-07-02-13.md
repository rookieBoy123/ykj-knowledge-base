# 2026-07-02 第13轮变更日志

## 新增业务流程

- **新增「处方结算流程（服务端）」**：完整描述处方从结算预览到创建订单的后端处理逻辑。覆盖预结算（处方状态校验→药房信息加载→收货地址查询→加工服务费确认→费用项逐项计算药品/加工/诊金/运费/医保→总金额汇总负数保护→订金模式判断药房开关+全局开关+最低门槛）、正式结算（已有订单检查→重新预结算→创建订单来源标记处方→支持第三方自定义订单号→回填处方订单号）、订单详情查询（基本信息+费用明细+支付渠道拼接微信/医保统筹/个账+收货地址快递模式+药房信息代煎服务费）、支付方式（微信/支付宝/医保统筹支付/医保个账支付/医保微信支付）、医保流程（授权码获取→医保支付→明细上传）、报销明细PDF生成。关联模块：ykj-service-order/ykj-service-yb/ykj-service-member/ykj-service-medicine。代码来源：PrescriptionSettlementServiceImpl.java（901行）。

- **新增「病历管理流程（服务端）」**：完整描述病历全生命周期管理的后端逻辑。覆盖病历创建（医生信息+患者信息+病历类型自动判断中医/西医+签名状态初始未签+主表写入+诊断子表写入+创建CA签署任务+运营模式自动完成签名）、病历更新（签名状态重置→主表更新→诊断差异对比删除/新增→诊断删除前绑定检查不可删关联处方诊断→新签署任务创建）、诊断绑定（处方关联后锁定不可改）、病历签署（CA电子签章生成标识+严格模式手写签名+运营模式自动签医生姓名）、多维度查询（患者/会员/签名状态/仅查有问诊的病历/分页创建时间倒序）、病历撤销（逻辑删除+撤销原因+拦截器链检查）、会员绑定（已绑定不允许重新绑定+支持更换患者）。异常处理：删除绑定诊断拦截提示诊断名称、会员重复绑定拦截、医生不存在提示。关联模块：ykj-service-ca/ykj-service-member/ykj-service-hospital。代码来源：MedicalRecordServiceImpl.java（851行）。

- **新增「药品管理流程（服务端）」**：完整描述药房药品管理的后端逻辑。覆盖药品双分类（成药专利药+中药草药+按药房归属）、成药添加（唯一性校验药房+规格+批准文号+重复提示）、中药添加（重复校验提示）、医生端搜索（黑名单关键字命中管制药品直接拦截提示不可线上开方+药房授权过滤仅展示被授权药房+库存状态标注+拼音搜索相似度排序）、编辑药品（不可修改唯一标识字段）、药品图片（OSS存储+自动组装URL）、Excel批量导入（中药模板+成药模板+覆盖更新策略已有则更新否则新增+单条失败不影响整体+错误记录）、第三方拉取（对接阿康等供应商+分页每页100条+7秒超时+同步标准药品库）、标准库关联（库存单位+换算比例+库信息不存在异常）、费用公示查询（公开查询+按药房+按类型+无需登录）。异常处理：管制药品拦截提示+药品已存在重复提示+药品库信息不存在异常+药房ID为空提示。关联模块：ykj-service-medicine/黑名单管理/标准药品库/第三方供应商。代码来源：MedicalDrugsServiceImpl.java（922行）。

## FAQ新增（8条）

- 215. 为什么处方结算时提示"已存在订单信息"？
- 216. 为什么病历更新后需要重新签名？
- 217. 为什么处方结算时总金额可能为0？
- 218. 为什么搜索药品时会提示"该药品为精麻毒等管制类药品"？
- 219. 为什么处方结算时有的订单只需要支付一部分（订金）？
- 220. 为什么删除诊断时提示不允许删除？
- 221. 为什么病历创建后医生签名状态是"未签"？
- 222. 为什么导入药品时有些药品失败了？
