# 变更日志 2026-07-03 (第14轮)

## 新增业务流程
1. **开放平台订单全生命周期流程（服务端）** — 分析 `OpenOrdersController.java`（292行）及 `OpenOrderServiceImpl.java`（831行）。涵盖预结算→锁定库存→创建订单→支付→发货→取货→取消的完整链路、机构校验（AppId/下单权限/关联药房）、药品库存校验（同药房限制、第三方同步）、费用计算（药品金额+运费）、预结算单有效期机制（默认5分钟）、第三方药房对接（AKANG）、库存增减操作、事件发布、操作日志记录。
2. **后台系统用户管理流程（服务端）** — 分析 `MgmtSysUserController.java`（292行）。涵盖13个接口：新增/编辑用户、修改用户名、分页查询、用户角色查询、检查用户名存在、重置密码、修改密码、获取当前用户（含角色+权限）、更换/查看手机号、启用/禁用用户。所有接口均通过 @PreAuthorize 控制权限（sys:users:read/write），通过 @Audit 记录操作审计日志。

## 新增常见问题
- 330：开放平台订单中，预结算和锁定库存为什么要分开两个接口？
- 331：开放平台为什么不直接返回药品库存不足，而要返回具体哪些药品不足？
- 332：后台系统用户的手机号查看为什么单独做成一个接口？
- 333：后台系统用户的"当前用户"接口和普通用户查询接口有什么区别？
- 334：开放平台订单的预结算单在什么时候会被清理？
- 335：后台管理系统的用户启停操作是否影响正在登录的用户？

## 代码来源
- `.../controller/OpenOrdersController.java` (292行)
- `.../service/impl/OpenOrderServiceImpl.java` (831行)
- `.../controller/mgmt/MgmtSysUserController.java` (292行)
