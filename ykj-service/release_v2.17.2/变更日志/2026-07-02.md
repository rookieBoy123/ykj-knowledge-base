# 2026-07-02 知识库更新

## 第十三轮深度分析

### 新增业务流程（4个）

- **新增「药品生命周期管理与第三方同步流程」**：完整描述药品从新增、编辑、导入、库存扣减到第三方药房自动同步的全生命周期后端逻辑。覆盖药品新增的三类唯一性规则（成药按编号+药房ID/中药按通用名）、第三方药房必填校验（进货价+来源编码）、黑名单检查、福建地区批准文号必填的地区适配校验、编辑时的不可修改字段保护（编号/药房/药房药品联合标识）、中药通用名唯一性校验、库存扣减的三重前置条件（库存管理开关+无限库存标记-1+库存不足日志）、三种第三方同步定时任务（药品信息拉取7s间隔+图片拉取+库存同步3s间隔）、Excel导入的逐行"先新增失败转编辑"策略、使用频次自动匹配、拼音首字母自动生成、药品删除后自动更新药房药品数量统计、医生端药房授权校验。代码来源：ykj-service-medicine/DrugsServiceImpl.java（848行）。

- **新增「医生视频内容管理流程」**：完整描述管理后台创建、编辑和管理医生科普视频内容的完整链路。覆盖新增时的四重校验（视频库存在+分类存在+医生存在+名称不重复）、编辑时的三重存在性检查+名称排己校验（排除自身ID）、名称同步更新到视频库、启用/禁用的即时生效、推荐标记的设置与取消、播放量的原子递增统计、患者端展示的启用状态过滤+分类/推荐可选筛选、医生选择列表的工作类型（Doctor）+启用状态双重过滤、分类列表查询。代码来源：ykj-service-hospital/DoctorVideoServiceImpl.java（431行）。

- **新增「问卷模板创建与问题库保存流程」**：完整描述医生创建和编辑问诊问卷的服务端完整逻辑。覆盖问卷编号自动生成（WJ+毫秒时间戳）、标题唯一性校验（同医生私有问卷+公用问卷双重规则）、标题/副标题300字长度限制、私有问卷转公用自动清除doctorId、题目全量替换策略（先删后增）、四层题目校验（文字非空+类型必选+序号必填+单选多选至少2选项）、问答题/量表题免选项校验、问题库同步的尽力而为策略（失败不影响问卷保存）、编辑时的所有权校验（小程序端医生只能改自己的）、列表查询的私有+公用混合展示策略、题目数量自动统计。代码来源：ykj-service-hospital/SurveyServiceImpl.java（390行）。

- **新增「量表发布校验与问诊中下发流程」**：完整描述量表从草稿发布到通过IM消息下发患者的全链路后端逻辑。覆盖发布前四重校验（题目存在+题目字段完整性+评分标准存在+评分标准字段完整性）、发布时的自动处理（上一题/下一题链表关系构建、单选题取最高分vs多选题取分值和计算总分、维度评分的分维度最高分累加与JSON保存）、发布幂等性（已发布重复调用不报错）、禁用状态保护（仅已发布可禁用）、评分类型切换的维度数据清理（维度→总分自动删除所有维度关联数据）、量表发送患者的四步流程（记录创建→问诊查找→系统消息→卡片消息异步发送）、测评重新评估的完成状态保护、卡片消息的按钮参数携带（recordId+scaleId跳转）。代码来源：ykj-service-scale/ScaleFullServiceImpl.java（375行）。

### 新增常见问题（12个）

- **FAQ #81**：为什么新增药品时提示"药品已存在"？但我在列表里看不到这个药品。（成药/中药不同的唯一性规则+药房作用域说明）
- **FAQ #82**：为什么Excel导入药品时，有些记录成功了、有些失败了？（逐行"先新增失败转编辑"策略+四种失败原因+修正重导建议）
- **FAQ #83**：为什么医生搜索中药时，有些药品搜不出来？（药房未授权/拼音为空/已禁用/搜索范围不含成药，四种原因）
- **FAQ #84**：第三方药房的库存数据多久同步一次？为什么有时库存显示不准确？（定时任务依赖+7s/3s间隔+同步失败原因+sourceCode匹配机制）
- **FAQ #85**：药品库存设为-1是什么意思？和设为0有什么区别？（-1无限库存/0缺货/正数可用+库存管理开关控制）
- **FAQ #86**：为什么新增医生视频时提示"视频不存在"？明明已经上传了。（DoctorVideo与Video两表独立+排查步骤）
- **FAQ #87**：医生视频设置了"推荐"，为什么在患者端没有排在前面？（isRecommend仅为标记字段+前端排序逻辑依赖）
- **FAQ #88**：量表发布时提示"量表题目信息未完善！缺失数据"（@InsertValid深度校验+选项分值/评分标准字段检查建议）
- **FAQ #89**：为什么量表从"维度评分"切换为"总分评分"后，之前设置的维度信息都没了？（数据清理机制+两种评分模式的数据隔离）
- **FAQ #90**：问卷编辑保存后，为什么原来的题目都不见了？（全量替换策略+先删后增机制+必须包含所有题目）
- **FAQ #91**：问卷题目"保存到问题库"失败会影响问卷保存吗？（尽力而为策略+失败提示机制+手动补充建议）
- **FAQ #92**：为什么医生在量表详情页看不到"发送给患者"的按钮？（问诊室中发送机制+依赖活跃问诊会话）

以上新增内容从以下核心服务深度分析得出：ykj-service-medicine/DrugsServiceImpl.java（药品管理，848行）、ykj-service-hospital/DoctorVideoServiceImpl.java（医生视频，431行）、ykj-service-hospital/SurveyServiceImpl.java（问卷管理，390行）、ykj-service-scale/ScaleFullServiceImpl.java（量表发布与发送，375行）。

## 第十四轮深度分析

### 新增业务流程（4个）

- **新增「订单支付渠道管理与重复支付防护流程」**：完整描述订单支付的全链路后端逻辑。覆盖四种支付渠道自动组合（微信JSAPI默认选中/微信扫码/支付宝/医保条件显示）、支付金额计算规则（含测试环境0.01元适配）、三重防重复校验（状态校验+历史渠道查单补单+预支付分布式锁）、渠道查单策略（按历史prepay单逐渠道查询、查单失败放行保体验、查到已支付同步补单后拦截）、预支付前清理（关闭旧预支付单+清除二维码缓存）、支付回调完成（入账分布式锁+流水幂等+重复支付自动退款+流水保存+状态推进三种分支）、取货码自动生成、已付金额溢出截断、医保退款（先医保退款+后微信退款+支出流水插入）。代码来源：ykj-service-order/OrderCommandServiceImpl.java（1251行）。

- **新增「处方结算费用明细计算流程」**：完整描述处方在支付前的预结算费用计算逻辑。覆盖六重处方状态校验（存在+未重复结算+医生已开方+审核通过+药师审方可选+未过期）、加工服务确认（医生已选→只能接受或取消、医生未选→患者自由选择、不可变更保护）、五项费用明细计算（药品金额固定第一+医事服务费可选合并+诊金+加工费标注服务名称+运费仅快递模式）、订金货到付款模式（药房启用+快递+系统开关+门槛金额四条件）、创建订单（药品×总剂数量组装+地址+运费+库存扣减）、开放平台库存不重复扣减保护。代码来源：ykj-service-inquiry/PrescriptionSettlementServiceImpl.java（901行）。

- **新增「问诊单激活与超时自动处理流程」**：完整描述问诊单从创建到激活、绑定、超时处理的完整链路。覆盖激活流程（启用聊天室+通知发送+可选自动接诊）、开放平台会员绑定（机构校验+状态校验+医生不能作为患者校验+已被领取保护）、重复问诊检测四种场景（商城/快速/预约/普通四种唯一性规则+自己不能问诊自己）、患者端四选项卡筛选逻辑（已预约=问诊中+挂号+未签到、问诊中=排除待签到、已完成、待支付=草稿）、超时自动处理定时任务（autoEnd开关+按flowStatus和doctorFlagStatus匹配+有服务医生时分主治转诊/助理取消+无服务医生直接结束）。代码来源：ykj-service-inquiry/InquiryAppointServiceImpl.java（918行）。

- **新增「病历签署CA同步与PDF生成流程」**：完整描述医生创建病历和电子签名的服务端完整逻辑。覆盖病历创建（医生信息组装+患者信息组装+病史信息医生优先策略+诊断非空/存在校验）、签署记录创建与CA判断双开关（CA基础服务+病历CA功能+主动禁用）、CA同步（查询CA账号+创建签署请求+同步第三方+返回uniqueId）、签署完成双模式（CA模式取Base64签章PDF+签名图片替换、普通模式生成PDF+末尾追加文字签名）、编辑时重新签署（诊断差异计算+已关联诊断保护禁止删除+disableValidDelBindingDiagnosis例外跳过）、病历撤销（断言校验+逻辑删除标记原因）。代码来源：ykj-service-inquiry/MedicalRecordServiceImpl.java（851行）。

### 新增常见问题（8个）

- **FAQ #93**：为什么支付时提示"当前订单已支付"但实际上刚打开支付页面？（历史渠道查单+自动补单机制说明）
- **FAQ #94**：订单支付时为什么会提示"支付处理中，请稍后重试"？（并发锁保护+5秒锁等待+触发场景）
- **FAQ #95**：为什么有些订单看不到支付宝支付选项？（三种默认渠道+默认选中+无订单类型限制）
- **FAQ #96**：处方结算时为什么提示"药师未审核处方，请稍后再试"？（先审后付模式+两种模式对比+操作建议）
- **FAQ #97**：为什么处方结算时提示"处方审核不通过或者已过期"？（三种失效原因+联系医生重新开方）
- **FAQ #98**：病历签署中的"CA签名"和"普通签名"有什么区别？（四维对比表+法律效力+操作步骤+签名样式+开启条件）
- **FAQ #99**：编辑病历时，为什么有些诊断删不掉？（关联保护+三种处理方案）
- **FAQ #100**：为什么同一个患者对同一个医生不能发起多次问诊？（四种场景的重复问诊限制规则对照表）

以上新增内容从以下核心服务深度分析得出：ykj-service-order/OrderCommandServiceImpl.java（订单支付，1251行）、ykj-service-inquiry/PrescriptionSettlementServiceImpl.java（处方结算，901行）、ykj-service-inquiry/InquiryAppointServiceImpl.java（问诊预约，918行）、ykj-service-inquiry/MedicalRecordServiceImpl.java（病历签署，851行）。

## 自动巡检（2026-07-02 定时任务）

**巡检结果**：24小时内无新增 commit，代码仓库无变更。

- 仓库1（小程序 ykj-wxminiprj）：无新提交
- 仓库2（管理后台 ykj-manager-analysis）：无新提交
- 仓库3（服务端 ykj-service）：无新提交（最近提交：2026-06-29，fangkai，MemberOrderDTO新增createTime字段）

今日无新增产品知识内容。

## 第十五轮深度分析

### 新增业务流程（4个）

- **新增「药品库管理与医生检索流程」**：完整描述药品从新增、编辑、导入导出、库存扣减到医生端搜索的全链路后端逻辑。覆盖中药/成药两套独立的药品库匹配规则（中药按通用名/成药按药监ID）、新增时的四重校验（药品库存在+药房内唯一+黑名单检查+福建批准文号地区适配）、编辑时不可修改字段保护（中药类通用名+名称+剂型+单位 / 成药的通用名+名称+剂型+分类+厂家+规格+单位）、Excel导入的逐行"先新增失败转编辑"策略、库存扣减的三重前置条件（药房库存管理开关+无限库存-1标记+单位换算比例）、医生搜索的拼音/中文双模式排序算法（Jaro-Winkler相似度+文本相似度+全量拼音匹配重排）、三种第三方同步定时任务、药房授权校验、黑名单搜索拦截。代码来源：ykj-service-medicine/MedicalDrugsServiceImpl.java（922行）。

- **新增「患者实名认证与就诊人绑定流程」**：完整描述患者从新增、实名认证、与会员绑定到医保信息同步的全链路。覆盖四种证件类型的分别校验（身份证算法+护照/港澳/台胞证正则）、非身份证强制填写性别生日、分布式锁防重复并发（姓名+身份证键值）、患者去重逻辑（已有但未实名→认证后补全 / 已有且已实名→跳过 / 不存在→新增）、身份认证第三方服务对接、身份证自动解析性别和出生日期、拼音首字母自动生成、本人关系唯一限制（一个会员只能一个本人）、患者唯一绑定限制（一个患者只能绑一个会员）、地址自动同步为收货地址、编辑时的身份去重合并机制、解绑的短信验证码流程、医保慢病信息同步（人员编号+险种+病种增量更新+I类病种自动设默认）。代码来源：ykj-service-hospital/PatientServiceImpl.java（815行）。

- **新增「医生排班管理与停诊处理流程」**：完整描述医生排班的完整后端逻辑。覆盖手动排班的六重校验链（班次名称冲突+班次时间冲突+号段时间超出范围+号段时间冲突+当天已过期号段操作限制+已结束号段号源数不可编辑）、排班模板自动同步定时任务（每天计算15天后日期+逐个医生生成排班）、一键发布所有未发布排班、停诊三重处理（全天停诊/班次停诊/停诊原因必填校验+100字限制）、号源使用与释放机制（预约前双重可用性校验+号段结束前30分钟禁止预约+退号号源+1且已退+1）、患者端查询过滤（仅返回已启用+已发布+未停诊班次）、停诊事件推送通知挂号患者。代码来源：ykj-service-hospital/DoctorScheduleServiceImpl.java（788行）。

- **新增「处方创建审核变更撤回全流程」**：完整描述处方从草稿到审核完成的完整状态机。覆盖草稿创建的双分支（新增/更新）+病历同步创建、正式提交的校验+病历关联+失效时间设置+电子处方PDF生成（A5纸张+印章加盖）、医生签章双路径（CA签名模式/非CA直接完成）、药师审核的四重校验（状态+时效+身份+专业匹配：西药师审核西药/中药师审核中药及中成药）、处方变更的患者不可变更保护+重新设置失效时间+变更标记+补建病历、撤回的已存在订单保护+撤销签章记录、删除的两种状态限制（仅草稿+已撤回）+医生权限校验、过期自动处理（双重状态更新+签章撤销）、诊断重置的全程禁用CA重签机制。代码来源：ykj-service-inquiry/PrescriptionCommandServiceImpl.java（750行）。

### 新增常见问题（10个）

- **FAQ #101**：为什么搜索中药时输入英文能搜出来但顺序很奇怪？（拼音相似度排序机制+全量匹配重排规则）
- **FAQ #102**：为什么医生A能在搜索框搜到某个中药，医生B搜不到？（药房授权限制+四种原因对照）
- **FAQ #103**：为什么新增药品时提示"药品库不存在该药品"？（两级校验机制+药品库/药房药品层）
- **FAQ #104**：为什么Excel批量导入药品后，有些成功有些失败？（逐行独立处理策略+四种失败原因+修正重导建议）
- **FAQ #105**：药品库存设为"-1"和"0"有什么区别？（三维对比：无限库存/零库存/正数+库存开关控制）
- **FAQ #106**：为什么身份证号码看起来正确，系统却提示"请输入正确的证件号码"？（四种证件类型校验规则+非身份证强制性别生日）
- **FAQ #107**：为什么添加就诊人时提示"关系为本人的就诊人只能添加一位"？（本人关系唯一限制+修改建议）
- **FAQ #108**：医生停诊后，为什么有些患者还能取消预约？（取消操作不校验停诊状态+号源释放逻辑）
- **FAQ #109**：处方提交后可以删除吗？（两种允许状态+医生权限+订单前置条件）
- **FAQ #110**：为什么药师审核处方时提示"您登记的药师专业不能审核当前类型的处方"？（西药师01/中药师02专业匹配规则+中成药例外）

以上新增内容从以下核心服务深度分析得出：ykj-service-medicine/MedicalDrugsServiceImpl.java（药品库管理，922行）、ykj-service-hospital/PatientServiceImpl.java（患者管理，815行）、ykj-service-hospital/DoctorScheduleServiceImpl.java（排班管理，788行）、ykj-service-inquiry/PrescriptionCommandServiceImpl.java（处方操作，750行）。

## 第十六轮深度分析

### 新增业务流程（3个）

- **新增「AI辅助问答与病历建议生成流程」**：完整描述医生在问诊中使用AI辅助功能的后端逻辑。覆盖两个独立的功能开关（问答辅助questionAiSuggestion/病历辅助medicalAiSuggestion）、AI输入数据的六个来源（问诊单+患者年龄性别+六维病史：过敏史/现病史/个人史/家族史/婚育史/月经史）、病史合并的医生优先策略（三个来源按优先级：预问诊→病历→患者档案）、防滥用三级机制（单次失败计数+3次失败触发锁+30分钟锁定时间）、聊天记录转换的角色识别（@doctor后缀=医生/@patient后缀=患者）、病历建议的诊断标准库自动匹配、无聊天记录时的默认病情描述兜底。代码来源：ykj-service-inquiry/SuggestionServiceImpl.java（453行）。

- **新增「AI医生代理服务全流程」**：完整描述AI医生独立问诊服务的完整后端链路。覆盖十个核心功能：开始问诊(start)、流式对话(chat/SSE流式传输)、问诊详情查询(detail/bizNo两种查询方式)、问题答案生成(genAnswer)、问诊报告生成(genReport)、记录分页查询(六维筛选：患者姓名/年龄范围/日期范围/业务类型/关联业务号)、对话历史查询(chatHistory)、问诊结束(finishPc)、处方图片OCR识别(doOcr/文件上传)、语音转写WebSocket地址获取(getSttWebSocketUrl)。统一的三层防护架构：功能开关（aiDoctorEnabled全局控制+关闭时所有接口返回默认值不报错）、统一错误处理（executeAgentHttp四校验：空响应/HTTP状态码/JSON解析/业务code）+统一降级机制（异常时返回默认值不中断业务）。代码来源：ykj-service-inquiry/AiAgentServiceImpl.java（433行）。

- **新增「退款申请审核与处理流程」**：完整描述退款从申请创建到审核、执行退款的完整状态机。覆盖退款申请创建的三重防重复校验（同订单+同支付流水+同自定义号）、退款单号自动生成（Refund+日期+7位随机数+碰撞重试）、审核双路径（同意→自动原路退款/手动标记 拒绝→必填备注）、原路退款的渠道自动分发（微信=WxRefundService/支付宝=AlipayPaymentService）、退款前四重校验（审核通过+状态允许+原路退回类型+申请单存在）、退款失败的重试与手动切换机制（retryRefund+manualRefund）、自动退款的事件驱动架构（OrderRefundApplyCreateEvent监听→自动同意退款）、跨渠道重复支付场景的支付渠道注入（瞬态字段payChannel）、退款金额的分→元转换、退款回调完成的事件发布（RefundApplySucceedRefundedEvent→通知订单模块）。代码来源：ykj-service-order/RefundApplyServiceImpl.java（423行）。

### 新增常见问题（6个）

- **FAQ #111**：为什么使用AI辅助问答时提示"系统暂不支持医生问答辅助"？（功能开关+两种AI辅助独立控制+联系管理员）

- **FAQ #112**：为什么AI问答辅助功能用了几次后突然不能用了，提示还有XX分钟才能使用？（3次失败锁定30分钟机制+原因+等待解锁建议）

- **FAQ #113**：AI辅助生成的病历建议中的诊断为什么有的有标准编码、有的没有？（诊断标准库匹配+名称不完全一致+保留展示）

- **FAQ #114**：AI医生代理功能开启后，为什么有的患者还是直接进入了人工问诊？（功能模块独立性+触发条件+场景依赖说明）

- **FAQ #115**：退款申请审批通过后为什么钱没有马上到账？（原路退回平台时间+手动退款线下时间+退款失败情况）

- **FAQ #116**：为什么订单退款被拒绝了？（审核备注说明+状态不符合+查看具体原因）

以上新增内容从以下核心服务深度分析得出：ykj-service-inquiry/SuggestionServiceImpl.java（AI辅助建议，453行）、ykj-service-inquiry/AiAgentServiceImpl.java（AI医生代理，433行）、ykj-service-order/RefundApplyServiceImpl.java（退款管理，423行）。

## 第十七轮深度分析

### 新增业务流程（3个）

- **新增「服务预约全生命周期流程」**：完整描述患者从选择服务项目到预约核销的全链路后端逻辑。覆盖项目可预约日期校验、费用计算的双分支（个人按服务费+上门费+非工作时间附加费计算 / 团队仅定金）、预约单创建的三分支（0元直接待核销+生成8位核销码 / 收费项目待付款 / 支付成功后自动转待核销）、核销的四重校验（预约单存在+订单存在+状态待核销+核销码匹配）、取消的双分支（待支付/0元直接取消 vs 已支付转退款中再退款）、超时自动取消监听机制（OrderCanceledEvent→自动同步预约单状态为已取消）、退款监听的双事件处理（OrderRefundApplyEvent→待退款 / OrderRefundEvent→已退款并清除核销码）、支付成功事件监听（生成核销码+推送通知）、核销成功后的自动开票触发（AutoBillingEvent→组装发票数据含收费项目明细分类汇总+支付渠道+患者HIS信息）、预约详情权限校验（本人或被分享人）、预约单号生成规则（R+yyyyMMdd+7位随机数）。代码来源：ykj-service-reservation/ServiceReservationsServiceImpl.java（826行）。

- **新增「随访任务定时发送与内容校验流程」**：完整描述随访任务从定时触发到患者接收的完整链路。覆盖定时任务触发→实时校验内容状态（患教查询课程发布状态/问卷查询发布状态→未发布内容标记forbidden）→判断是否发送（至少一项可发送→状态改为随访中+推送订阅消息 / 全禁用→自动取消）→内容状态更新持久化→消息推送（微信订阅消息+获取医生信息+获取会员openId+未能获取时跳过推送记录日志）→患者端接收（列表仅返回「随访中」和「已完成」两种状态→非法状态查询拦截）。患者端任务操作：小结添加（仅任务所有人可编辑）、内容项状态编辑（患教已读标记）、问卷答题提交（answerSheet）、内容详情查看（患教/问卷/提醒三种类型分别查询）。随访计划总览操作：添加总结（仅已完成状态允许）、取消随访、保存校验（防重复进行中随访）。代码来源：ykj-service-followup/FollowupTaskServiceImpl.java（197行）、ApiFollowupTaskController.java、ApiFollowupController.java。

- **新增「导诊记录同步与患者建档流程」**：完整描述医院线下导诊台录入患者信息、生成导诊编号、医生调取信息的完整后端逻辑。覆盖导诊记录创建（患者档案自动建档→UUID唯一编号生成→病情信息+过敏史+既往史+现病史+病历图片OSS转存→状态初始Unused→监护人信息加密存储+同导诊下同身份证号防重复）→医生调取使用（状态改为Used+记录使用医生ID→返回完整患者信息含监护人）→患者信息补充更新（重新建档+关联更新）。关键设计：一次性使用编号、监护人身份证加密、病历图片永久存储。代码来源：ykj-service-guidance/GuidanceServiceImpl.java（155行）。

### 新增常见问题（7个）

- **FAQ #117**：为什么服务预约提交后直接就显示"待核销"了，没有支付环节？（0元免费项目自动跳过支付+付费项目进收银台的双分支说明）
- **FAQ #118**：为什么取消已支付的服务预约后，状态变成了"退款中"而不是"已退款"？（异步退款流程+状态中间态说明+等待时间建议）
- **FAQ #119**：服务预约的核销码可以在哪里查看？（小程序预约详情+订阅消息两种查看方式+核销码格式说明）
- **FAQ #120**：为什么随访任务发送后，患者说没收到提醒？（四种原因：内容全禁用/未订阅消息/会员信息获取失败/医生异常+排查建议）
- **FAQ #121**：随访计划的总结报告什么时候可以添加？（仅已完成状态+进行中计划不允许+操作建议）
- **FAQ #122**：导诊编号生成后可以重复使用吗？（一次性使用设计+已使用后不可再调取+更新患者信息的例外机制）
- **FAQ #123**：为什么团队预约的费用只显示"定金"而不是全额？（定金模式+线下尾款结算+降低在线支付门槛说明）

以上新增内容从以下核心服务深度分析得出：ykj-service-reservation/ServiceReservationsServiceImpl.java（服务预约，826行）、ykj-service-followup/FollowupTaskServiceImpl.java（随访任务，197行）、ykj-service-guidance/GuidanceServiceImpl.java（导诊记录，155行）。
