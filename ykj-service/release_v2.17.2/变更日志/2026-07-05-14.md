# 变更日志 2026-07-05 第14轮

## 概述
本轮从服务端 `StdMedAssembleServiceImpl` 的校验/保存逻辑、近期 `CurrentDoctorVO` 新增字段、以及订单退款恢复机制中深挖边界情况，产出5条FAQ。同时与小程序和管理后台的跨系统分析配合，覆盖标准医疗信息库的后端业务逻辑。

## 新增常见问题（5条）

| 编号 | 问题 |
|------|------|
| 575 | 为什么编辑标准药品信息后说明书内容不见了？ |
| 576 | 为什么标准医疗信息列表里看不到我新录入的药品？ |
| 577 | 为什么服务器重启后支付宝支付开关状态会自动更新？ |
| 578 | 为什么退款被拒绝后订单恢复到了退款前的状态？ |
| 579 | 为什么医生API接口返回的数据中新增了「会诊类型」字段？ |

## FAQ来源反推逻辑

| FAQ编号 | 反推角度 | 对应代码 |
|---------|---------|---------|
| 575 | StdMedAssembleServiceImpl 的 saveOrUpdate 逻辑 → 空字符串 vs null 覆盖行为差异 | `StdMedAssembleServiceImpl.java`（save/update方法） |
| 576 | 前端路由缓存策略 + 后端权限校验 | `MgmtStdMedController.java`（@PreAuthorize） |
| 577 | @RefreshScope 动态刷新机制 | `AliPaymentServiceImpl.java`、`settings.yaml` |
| 578 | restoreOrderFlowStatus 安全恢复机制+origin_flow_status保护 | `OrderCommandServiceImpl.java`（1145-1161行） |
| 579 | CurrentDoctorVO 近期新增字段 → 前端接诊模式判断 | `CurrentDoctorVO.java`（consultationType/isDelayCheckIn） |

## 代码来源

- `StdMedAssembleServiceImpl.java`（124行）— save/update/detailsByMedId 方法
- `MgmtStdMedController.java`（96行）— 标准药品CRUD接口
- `AliPaymentServiceImpl.java` — @RefreshScope 动态刷新
- `OrderCommandServiceImpl.java`（1145-1161行）— restoreOrderFlowStatus
- `CurrentDoctorVO.java` — consultationType、isDelayCheckIn 字段
