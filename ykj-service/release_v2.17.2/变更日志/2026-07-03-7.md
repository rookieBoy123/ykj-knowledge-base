# 知识库更新日志 2026-07-03（第7轮）

## 本日新增

### 新增业务流程文档（2篇）

- **新增「标准药品诊断对照管理流程（服务端）」**：完整描述标准药品与诊断关联对照的后端管理流程。覆盖5个接口：分页查询（GET /mgmt/std/stdMedDiag）+ 新增（POST，权限 `std:med-diag:write`，同名药品智能合并提示）+ 修改（PUT，药品通用名不可改）+ 详情（GET /detail）+ 批量导入（POST /import，权限 `std:med-diag:import`，Excel格式要求）。服务模块 `ykj-service-std`。代码来源：MgmtStdMedDiagnoseController.java（66行）。

- **新增「标准药品信息管理流程（服务端）」**：完整描述标准药品基础数据的后端管理流程。覆盖分页查询（条件筛选药品通用名/标准编码）+ 新增（POST，录入药品通用名/编码/剂型/规格）+ 修改（PUT）+ 详情（GET）+ 批量导入（Excel）。标准药品是整个系统药品管理的基准数据，药房药品通过标准药品关联。服务模块 `ykj-service-std`。代码来源：MgmtStdMedController.java（96行）。

### 新增常见问题（3条）

- FAQ #274-#276：覆盖标准药品诊断对照同名药品智能合并提示的含义与操作、标准药品与药房药品的区别（字典vs商品）、批量导入失败的可能原因（格式/权限/数据不存在）。

### 覆盖度变化

- 业务流程：106 → 108 篇
- 常见问题：273 → 276 条
