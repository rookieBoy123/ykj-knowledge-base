# 变更日志 2026-07-07

## 概述
今日服务端仓库无新增提交，代码与昨日保持一致。

## 代码变更清单
无

## 总结
今日无变更。

---

## 第二轮挖掘 (ServiceImpl深度FAQ)

### 新增常见问题
- **问题 710**：为什么药师审核处方时提示"当前处方不能被审核"？ — 从 `PrescriptionCommandServiceImpl.audit()` 反推，覆盖处方审核状态流转条件
- **问题 711**：为什么药师审核处方时提示"非药师身份或者当前药师不可用，不能审核处方"？ — 从 `PrescriptionCommandServiceImpl.audit()` 反推，覆盖药师角色+绑定校验
- **问题 712**：为什么处方结算时提示"已存在订单信息"？ — 从 `PrescriptionSettlementServiceImpl.settlement()` 反推，覆盖防重校验机制

### 代码来源
- `OrderCommandServiceImpl.java` (1308行)：prepay() 分布式锁、preparePayment() 支付状态校验、completeOrder() 主动完成校验
- `PrescriptionCommandServiceImpl.java` (750行)：audit() 审核状态流转+药师角色校验
- `PrescriptionSettlementServiceImpl.java` (901行)：settlement() 防重校验、preSettlement() 预结算
- `MedicalDrugsServiceImpl.java` (922行)：精麻毒黑名单校验、福建地区批准文号必填、药品重名校验
