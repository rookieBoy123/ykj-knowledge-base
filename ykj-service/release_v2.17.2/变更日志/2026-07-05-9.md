# 变更日志 2026-07-05（第9轮）

## 新增业务流程（1篇）

| 文档 | 来源 |
|------|------|
| 支付宝支付切换与预支付单作废流程（服务端） | `ApiOrderController.java`（cancelPayment/expire接口）+ `OrderCommandServiceImpl.java`（cancelPayment/expire/restoreOrderFlowStatus/completeOrder/paymentCallback） |

## 新增常见问题（7条）

| 编号 | 问题 | 来源 |
|------|------|------|
| 534 | 为什么切换支付方式后，原来的预支付单会自动作废？ | OrderCommandServiceImpl：cancelPayment 方法 |
| 535 | 为什么预支付时显示"订单已失效"，但订单状态还是待支付？ | OrderCommandServiceImpl.advance()：过期时间拦截 + ApiOrderController.expire() |
| 536 | 为什么取消订单时提示"订单状态已变更，取消失败"？ | OrderCommandServiceImpl.cancelOrder()：乐观锁并发控制 |
| 537 | 为什么订单取消时，有多笔收入流水的处理方式和单笔流水不同？ | OrderCommandServiceImpl.cancelOrder()：多流水逐条退款 + 跳过已退款流水 |
| 538 | 为什么退款失败或驳回后，订单状态可以从"退款中"恢复到之前的状态？ | OrderCommandServiceImpl.restoreOrderFlowStatus()：originFlowStatus 机制 |
| 539 | 为什么手动完成订单会触发额外的业务事件？ | OrderCommandServiceImpl.completeOrder()：OrderCompletedEvent 发布 |
| 540 | 为什么重复支付不会导致用户被多扣款？ | OrderCommandServiceImpl.handlePaymentCallback()：重复支付自动退款 + 跳过已退款流水 |

## 统计

- 业务流程文档：187 → 188 篇（+1）
- 常见问题：533 → 540 条（+7）

## 代码来源

```bash
# 服务端 - 订单模块
ApiOrderController.java                  # 接口层：_cancel-payment、expire、_payment-status 等新增接口
OrderCommandServiceImpl.java             # 核心业务：cancelPayment、expire、cancelOrder(多流水)、restoreOrderFlowStatus、completeOrder(事件)、paymentCallback(重复支付)
OrderCommandService.java                 # 接口定义

# 关联分析用
ApiOrderController.java                  # cancel-payment/expire/payment-status 接口变更
MgmtLoginController.java                 # 管理后台登录（线下药房+AccessKey双模式）
MgmtSysAuditLogController.java           # 操作日志（客户端类型：MANAGER/OFFLINE）
MgmtSysScheduledJobController.java       # 定时任务管理（jobType区分可编辑性）
MgmtSysNotificationController.java       # 消息通知中心（长轮询机制）
MgmtChangeRoleController.java            # 角色切换（Token重新生成）
```

## 说明

本轮重点挖掘了 v2.17.2 中订单支付模块的最新变更（支付宝支付优化、订单过期机制、多流水退款、乐观锁保护等）。这些变更来自最近的 git 提交（完善对支付宝支付的支持），此前知识库尚未覆盖这些新增的保护机制和优化逻辑。同时从系统管理模块（登录、审计日志、定时任务、通知中心、角色切换）中反推了5条面向管理员的常见问题。
