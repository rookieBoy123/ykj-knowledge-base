# 变更日志 2026-07-04（第11轮）

## 新增业务流程（2篇）

### 1. 运费规则设置与实时计算流程（服务端）
- 文件: `业务流程/运费规则设置与实时计算流程（服务端）.md`
- 功能: 管理端设置+患者端实时匹配的运费计算体系，含地区优先级匹配和三种计费模式
- 代码来源:
  - `MgmtFreightController.java`（53行）— 管理端运费设置接口
  - `FreightServiceImpl.java`（130行）— 运费存储+缓存刷新+地区互斥校验
  - `ApiFreightServiceImpl.java`（117行）— 患者端运费实时计算（城市→省份→其他地区三级匹配）
  - `FreightItems.java`（62行）— 运费规则数据模型
- 接口: `/mgmt/freight` (GET/POST), `calculateFreight()`, `isGratisEnabled()`
- 关键设计: Redis缓存（key: `freight:items`）、synchronized锁保护并发刷新、HashSet地区去重校验

### 2. 科室层级管理与HIS同步流程（服务端）
- 文件: `业务流程/科室层级管理与HIS同步流程（服务端）.md`
- 功能: 树形科室结构管理，支持普通模式和互联网诊疗HIS同步双模式
- 代码来源: 管理后台 `unitconfiguration/index.vue`（495行）对应的服务端接口
- 接口: `/mgmt/departments/tree`, `/mgmt/departments`, `/mgmt/departments/{id}`
- 关键设计: parentId树形关联、双模式切换（internetDiagnosis标志）、insuranceName自动填充冗余、code+parentId不可修改约束

## 新增常见问题（6条）

| 编号 | 问题 |
|------|------|
| FAQ #433 | 运费规则设置后，为什么患者下单时匹配不到正确的运费？ |
| FAQ #434 | 医生在开处方时，为什么有的能免运费、有的不能？ |
| FAQ #435 | 为什么修改运费规则后提示"一个地区只能设置一个条件"？ |
| FAQ #436 | 运费设置为0元（包邮）和开启满额包邮有什么区别？ |
| FAQ #437 | 运费规则保存成功后，为什么患者下单时仍然显示旧的运费？ |
| FAQ #438 | 为什么同一用户多次下单，运费金额不一样？ |

## 统计
- 业务流程总篇数：164 → 166（+2）
- FAQ总条数：432 → 438（+6）
